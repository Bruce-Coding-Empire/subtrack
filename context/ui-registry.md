# UI Registry

Living document. Updated after every component is built, on both web and mobile. Read this before building any new component — match existing patterns exactly before inventing new ones.

---

## How to Use

Before building any component:

1. Check if a similar component already exists here (on the same platform — web and mobile registries are tracked separately below since implementations differ, even when the visual result matches)
2. If yes — match its exact classes/structure
3. If no — build it following `ui-rules.md` and `ui-tokens.md`, then add it here

After building any component — update this file with the component name, file path, and exact classes/props used.

---

## Web Components

### `Input` — `apps/web/components/ui/input.tsx`

Hand-written (not shadcn CLI-generated) to hit `ui-rules.md`'s Form Input spec exactly. Base classes: `w-full rounded-md border border-border bg-surface px-3 py-2 text-sm text-text-primary placeholder:text-text-muted transition-colors outline-none focus:border-accent focus:ring-1 focus:ring-accent disabled:cursor-not-allowed disabled:opacity-50 aria-invalid:border-error`. Props: standard `<input>` props, `className` merged via `cn()`.

### `PasswordInput` — `apps/web/components/ui/password-input.tsx`

Wraps `Input` with a show/hide toggle. `type` alternates between `password`/`text` via local `isVisible` state; forwards all other props (including react-hook-form's `field` spread) straight to `Input`. Toggle is a `type="button"` (never submits the form) positioned `absolute inset-y-0 right-0`, icon-only (`Eye`/`EyeOff` from `lucide-react`, `size-4`), `tabIndex={-1}` (not part of tab order — the input itself is), `aria-label` swaps between "Show password"/"Hide password". `Input`'s className gets `pl-3 pr-9` to keep left padding at spec (12px, same as base `px-3`) while making room for the icon on the right. Used by both `LoginForm` and `RegisterForm` for their password field.

### `Label` — `apps/web/components/ui/label.tsx`

shadcn CLI-generated (`npx shadcn add label`), unmodified. Plain `<label>` wrapper, no external primitive dependency — matches this project's Base UI (not Radix) posture. Used directly by `FormLabel` below; not typically used standalone since `FormField`/`FormLabel` covers every current form field.

### `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogAction`, `AlertDialogCancel` — `apps/web/components/ui/alert-dialog.tsx`

shadcn CLI-generated (`npx shadcn add alert-dialog`), unmodified — `base-vega` resolves it cleanly like `Table`/`Dialog`/`Select`/`Badge`/`Pagination`. Built on `@base-ui/react`'s `AlertDialog` primitive, same shape as `Dialog` (`AlertDialogContent` composes `AlertDialogPortal` + `AlertDialogOverlay` + `Popup`). Ships a working `AlertDialogTrigger`, but this codebase's manual-open-state convention (see `AddSubscriptionDialog` below) is used instead for `CancelSubscriptionDialog`/`DeleteSubscriptionDialog`, for consistency with the existing `Dialog` usages rather than because the trigger doesn't work.

### `Form`, `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormDescription`, `FormMessage` — `apps/web/components/ui/form.tsx`

Hand-written, not CLI-generated — this project's shadcn registry/style (`"base-vega"`) resolves `@shadcn/form` to zero files (verified via `npx shadcn add @shadcn/form --view` → "No files"), so the canonical Form primitives don't exist for this style. Rebuilt to match shadcn/ui's standard Form API (`react-hook-form` + `FormProvider`/`Controller`/`useFormContext`/`useFormState`), with one deliberate deviation: `FormControl` uses a local `React.cloneElement`-based forwarder instead of Radix's `Slot` (this project has no Radix dependency — Button/Card/Input are all Base UI or hand-written), since it only ever wraps a single input-like child. `FormLabel` renders the CLI-generated `Label` above with `data-error` wired to `text-error` per `ui-rules.md`. `FormMessage` renders at `text-xs text-error`, matching the inline field-error styling every other form in this project uses. New deps: `react-hook-form`, `@hookform/resolvers` (see `code-standards.md`'s Dependencies section for why these are approved).

Also note: `buttonVariants`' base classes gained `cursor-pointer` and `disabled:cursor-not-allowed` (was missing on all variants — shadcn's generated `Button` didn't set either, so hover showed the default arrow cursor instead of a pointer). Applies to every `Button` usage app-wide, not just auth forms.

### `LoginForm` — `apps/web/components/auth/LoginForm.tsx`

Client component. `useForm` (react-hook-form) + `zodResolver`, schema mirrors `LoginDto` (`z.email()` for email, non-empty for password). Each field is a `FormField` → `FormItem` → `FormLabel` + `FormControl(Input)` + `FormMessage`. A separate form-level error (`text-xs text-error`, not part of the Form primitives) surfaces API failures (wrong password, network error) since those aren't per-field zod issues. Submit button: `Button` with `bg-accent text-accent-foreground hover:bg-accent-dark w-full`, disabled via `form.formState.isSubmitting`. Calls `lib/auth.ts`'s `login()`, redirects to `/dashboard` via `useRouter().push()` on success.

### `RegisterForm` — `apps/web/components/auth/RegisterForm.tsx`

Same pattern as `LoginForm`, plus a `name` field (zod: non-empty, max 100 chars, mirrors `RegisterDto`). Calls `lib/auth.ts`'s `register()`.

### Auth page shell — `apps/web/app/(auth)/layout.tsx`

Centered flex column, `bg-background`, small branded header above the form: 36×36px logo box (`rounded-[10px]`, `bg-[linear-gradient(135deg,var(--color-accent)_0%,var(--color-accent-dark)_100%)]` — the one approved gradient per `ui-rules.md`) + "SubTrack" wordmark (`text-[19px] font-bold text-text-primary`, per `ui-tokens.md`'s Logo text row). Children rendered inside a `max-w-sm` wrapper. `/login` and `/register` both render a shadcn `Card` (`CardHeader` + `CardTitle` + `CardDescription` + `CardContent`) inside this shell — no changes needed to `Card` itself, token remap in `globals.css` makes it match `ui-rules.md`'s Cards spec (white surface, `rounded-xl` = 16px, `p-6` = 24px, shadow via the remapped `--shadow-xs`).

### `Table`, `Dialog`, `Select`, `Badge` — `apps/web/components/ui/{table,dialog,select,badge}.tsx`

shadcn CLI-generated (`npx shadcn add table dialog select badge`), unmodified — unlike `Form`/`Input`, this style (`base-vega`) does resolve these. Built on `@base-ui/react` primitives (`Select`, `Dialog`), consistent with `Button`. All read the same semantic vars remapped in `globals.css` (`--border`, `--muted`, `--popover`, etc.), so they match `ui-rules.md` with only per-usage `className` overrides (e.g. `TableHead` needs `text-xs font-medium uppercase text-text-secondary` added per-instance — the CLI default is `text-foreground`, not uppercase/secondary). `TableRow`'s default `hover:bg-muted/50` already equals `hover:bg-surface-secondary` from the remap, so no override needed there. **Gotcha:** `Select`'s `SelectValue` renders the raw `value` string by default (not the matching `SelectItem`'s label) — pass a function child, `<SelectValue>{(value) => LABEL_MAP[value]}</SelectValue>`, everywhere the select's value and its display label differ (e.g. category `"software"` → "Software"; not needed for currency, where the code and label are identical).

Re-running `npx shadcn add` for a batch that includes `Button` will silently drop the `cursor-pointer`/`disabled:cursor-not-allowed` fix from the Post-04 follow-up (the CLI overwrites the whole file) — re-add both classes to `buttonVariants`' base string if this happens again.

### `CategoryBadge`, `StatusBadge` — `apps/web/components/subscriptions/{CategoryBadge,StatusBadge}.tsx`

Both wrap shadcn `Badge` with `variant="outline"` (to null out the default `bg-primary`) plus a per-value `className` override. `CategoryBadge` uses `bg-category-{name}/12 text-category-{name}` (Tailwind v4's opacity-modifier syntax against the `--color-category-*` vars in `globals.css`) per `ui-rules.md`'s "12% opacity, category token" badge rule — no dedicated `-light` category tokens exist, so the `/12` opacity suffix is the fallback the rule itself specifies. `StatusBadge` uses the literal `bg-success-light`/`bg-surface-secondary` tokens `ui-rules.md` names directly.

### `RenewalUrgencyPill` — `apps/web/components/subscriptions/RenewalUrgencyPill.tsx`

Implements `ui-rules.md`'s Renewal Urgency Indicators table (`lib/renewal-urgency.ts`'s `getRenewalUrgency()`: ≤7 days → `warning`, 8–30 → `info`, overdue → `error`, else no pill — just plain date text). Used on the subscription detail page's "Next Renewal" field, per the rule's stated scope ("upcoming renewals list and subscription detail") — not used in the subscriptions table, which only shows a plain formatted date plus the separate Status column.

### `SubscriptionsTable` — `apps/web/components/subscriptions/SubscriptionsTable.tsx`

Client component (`useRouter` for row-click navigation to `/subscriptions/[id]`). Columns exactly per `ui-rules.md`: Name, Category (badge), Cost, Cycle, Next Renewal, Status — no actions column, since none is specified. Empty state is a plain centered muted-text message (no icon/CTA — the page-level "Add Subscription" button already covers that action, so a duplicate CTA inside the empty state would be redundant).

### `SubscriptionsPagination` — `apps/web/components/subscriptions/SubscriptionsPagination.tsx`

shadcn `Pagination`/`PaginationContent`/`PaginationItem`/`PaginationPrevious`/`PaginationNext` (`npx shadcn add pagination`), used in a client-state-driven way rather than the CLI default's URL-link model — `PaginationPrevious`/`Next` render as `<a href="#">` under the hood, so `onClick` calls `event.preventDefault()` before invoking the `onPageChange` callback, and the disabled edge (page 1 / last page) is done via `aria-disabled` + `pointer-events-none opacity-50`, not a real `disabled` attribute (anchors don't have one). No numbered page links or ellipsis — just Previous / "Page X of Y" / Next, since the subscriptions list is never expected to need deep pagination.

### `SubscriptionsPageClient` pagination — `apps/web/components/subscriptions/SubscriptionsPageClient.tsx`

`DEFAULT_PAGE_SIZE = 10`, sent as the real `limit` query param to `GET /subscriptions` (feature 07 — previously a client-side slice of a mock array; `total`/`totalPages` now come from the server response). Changing the search query or status filter resets to page 1 (via wrapper handlers `handleSearchChange`/`handleStatusChange`, not a `useEffect` — simpler and avoids an extra render). The pagination footer (in a `CardFooter`, `border-t`) only renders when there's more than one page — no inert "Page 1 of 1" bar when everything fits on one page. **Feature 07 additions:** the search input is debounced 300ms (`SEARCH_DEBOUNCE_MS`, a second `debouncedSearch` state synced from `search` via a `setTimeout`/`clearTimeout` effect) before it's sent as the `search` query param, so real API requests aren't fired on every keystroke. The actual fetch (`listSubscriptions({ status, search, page, limit })`) lives entirely inside one `useEffect`, as an `async function load()` declared and invoked *inside* the effect body rather than a `useCallback` called from the effect — `eslint-plugin-react-hooks@7`'s new `react-hooks/set-state-in-effect` rule flags the `useCallback`-then-call-from-effect shape as "calling setState() directly within an effect" (it traces the callback's setState calls through to the effect), even though the calls only actually run after an `await`; defining the async function inline avoids the false positive. A `refreshToken` counter (bumped by `handleCreated`, included in the effect's dependency array) is how `AddSubscriptionDialog`'s `onCreated` callback triggers a re-fetch without needing to expose the fetch function itself outside the effect.

### `SubscriptionFilters` — `apps/web/components/subscriptions/SubscriptionFilters.tsx`

Search `Input` (with a `lucide-react` `Search` icon absolutely positioned inside, left-padded) plus an All/Active/Cancelled segmented control — same hand-rolled segmented-control pattern as `SubscriptionForm`'s billing cycle control (plain buttons, active = `bg-accent text-accent-foreground`, inactive = `text-text-secondary hover:bg-surface-secondary`), not a shadcn primitive, since shadcn's registry has no toggle-group component for this style.

### `SubscriptionForm` — `apps/web/components/subscriptions/SubscriptionForm.tsx`

Shared by both the Add dialog and the Edit dialog — `defaultValues`/`onSubmit`/`submitLabel`/`onCancel`/`formError` props, same `react-hook-form` + `zodResolver` + shadcn `Form` pattern as `LoginForm`/`RegisterForm`. Billing cycle is the same hand-rolled segmented control as the status filter (4 options); selecting "Custom" reveals an "Every N Days" field and auto-fills it with `30` if empty (`form.setValue`) so the field is never left blank when first revealed. **Numeric fields (`cost`, `customIntervalDays`) do not use `z.coerce.number()`** — mixing `z.coerce` with `react-hook-form`'s `useForm<T>()` generic breaks the resolver's type inference (input type becomes `unknown` vs. output `number`, a known `zodResolver` + strict-TS gotcha). Fixed by keeping the zod schema as plain `z.number()` and wiring each numeric `Input` manually (`value`/`onChange={(e) => field.onChange(e.target.valueAsNumber)}`) instead of spreading `{...field}`. Watches `billingCycle` via `useWatch({ control: form.control, name: "billingCycle" })`, not `form.watch()` — `form.watch()` triggers a React Compiler "cannot be memoized" lint warning; `useWatch` is the proper hook form and doesn't. **`formError`** (added in feature 07): optional string rendered as `role="alert" text-xs text-error` just above the button row, same pattern as `LoginForm`/`RegisterForm`'s form-level API-error text — surfaces `POST`/`PATCH /subscriptions` failures, which aren't per-field zod issues. The `Cancel` button (rendered when `onCancel` is passed) is styled Primary (`bg-accent text-accent-foreground hover:bg-accent-dark`, no `variant="outline"`), matching `submitLabel`'s button — see `ui-rules.md`'s Buttons section for why this codebase has no secondary/outline button style. `onSubmit` is typed `(values) => void | Promise<void>` since the Add/Edit dialogs now `await` a real API call inside it; `react-hook-form`'s `form.handleSubmit` awaits it automatically, so `form.formState.isSubmitting` (already wired to the submit button's `disabled`) keeps working with no extra prop needed.

### `AddSubscriptionDialog` — `apps/web/components/subscriptions/AddSubscriptionDialog.tsx`

shadcn `Dialog` wrapping `SubscriptionForm`. **Feature 07:** `onSubmit` now calls `lib/subscriptions.ts`'s `createSubscription()` (a real `POST /subscriptions`) instead of building a client-side `Subscription` with `crypto.randomUUID()` — `nextRenewalDate` is calculated server-side per `api-contract.md`, never sent from the client. On success, closes the dialog and calls the new `onCreated: () => void` prop (was `onCreate: (subscription) => void`) so the parent list re-fetches instead of receiving a fabricated item. On failure, sets a local `formError` state passed straight to `SubscriptionForm`, cleared on dialog re-open (`onOpenChange`).

### `EditSubscriptionDialog` — `apps/web/components/subscriptions/EditSubscriptionDialog.tsx`

Same shape as `AddSubscriptionDialog` (shadcn `Dialog` wrapping `SubscriptionForm`, `Button` sits as a `Dialog` sibling rather than a `DialogTrigger` — open state is driven manually via `useState` + `onClick`, matching the Add dialog's existing pattern). Takes `subscription` + `onSaved` props instead of building a subscription from scratch — `defaultValues` is populated from the current subscription so the form opens pre-filled. Triggered by an "Edit" button (`Pencil` icon) in the detail page's header row, replacing what was originally an always-visible inline edit `Card` on the detail page — the modal keeps the page's default (read-only) state uncluttered and matches the Add flow's UX. Styled Secondary (`border-accent bg-surface text-accent hover:bg-accent-light`, no `variant="outline"`) — it's a modal trigger that isn't the page's main action, next to the page's Destructive `Cancel Subscription`/`Delete` buttons, so it uses the Secondary pattern per `ui-rules.md`'s Buttons section rather than Primary. **Feature 07:** `onSubmit` now calls `updateSubscription(subscription.id, values)` (a real `PATCH /subscriptions/:id`); the prop changed from `onSave: (values) => void` to `onSaved: (subscription: Subscription) => void`, passing back the server's response (with its recalculated `nextRenewalDate`, if `billingCycle`/`startDate` changed) so the detail page merges it into local state rather than echoing the raw form values. Same `formError` handling as `AddSubscriptionDialog`.

### `CancelSubscriptionDialog` — `apps/web/components/subscriptions/CancelSubscriptionDialog.tsx`

New in feature 07. The trigger button (opens the dialog) is styled as the outlined Destructive trigger per `ui-rules.md` — `border-error bg-surface text-error hover:bg-error-light`, **no** `variant="outline"` — since the destructive action hasn't happened yet at that point; the actual confirm button inside the dialog stays solid Destructive per `ui-tokens.md`'s spec (`bg-error text-white` — literal `text-white`, not a project token; this is the one spot in the codebase where a raw Tailwind color class is correct, since the token doc itself specifies "text: white" rather than naming a token). `AlertDialog` follows the same manual-open-state pattern as `Dialog` usages elsewhere (not `AlertDialogTrigger`), for consistency. Only rendered by `SubscriptionDetailClient` when `subscription.status === "active"` (cancelling an already-cancelled subscription is a no-op the UI shouldn't offer). Confirmation footer: "Keep Subscription" (Secondary style — `border-accent bg-surface text-accent hover:bg-accent-light`, **no** `variant="outline"`; see `ui-rules.md`'s Buttons section for why Secondary is built this way rather than via the outline variant) + a second "Cancel Subscription" button (same label as the trigger — tests/automation must scope queries to `[data-slot="alert-dialog-content"]` to disambiguate) that calls `cancelSubscription(id)`; on success passes the updated `Subscription` up via `onCancelled`, on failure shows an inline `role="alert"` error inside the dialog without closing it.

### `DeleteSubscriptionDialog` — `apps/web/components/subscriptions/DeleteSubscriptionDialog.tsx`

New in feature 07. Same trigger/confirm styling split and manual-`AlertDialog` pattern as `CancelSubscriptionDialog` (outlined Destructive trigger, solid Destructive confirm). Always rendered (delete is offered regardless of status; the block is enforced server-side, not hidden client-side). Confirmation footer's "Keep Subscription" is styled Secondary, same reasoning as `CancelSubscriptionDialog`. On confirm, calls `deleteSubscription(id)`; on success calls `onDeleted()` (the detail page routes back to `/subscriptions`). On failure — i.e. the subscription has `payment_history` rows — the API's exact message ("This subscription has payment history and cannot be deleted — cancel it instead") is shown inline in the dialog via `role="alert"`, matching `api-contract.md`'s "direct the user to cancel instead" requirement verbatim rather than a generic error string.

### `PaymentHistoryList` — `apps/web/components/subscriptions/PaymentHistoryList.tsx`

Simple two-column shadcn `Table` (Date, Amount); empty state is plain centered muted text ("No payment history yet.") — no CTA, since there's no user action that creates payment history (it's system-generated by the renewal job).

### `SubscriptionsPageClient` / `SubscriptionDetailClient` — `apps/web/components/subscriptions/{SubscriptionsPageClient,SubscriptionDetailClient}.tsx`

Client components rendered by the (Server Component) `app/subscriptions/page.tsx` and `app/subscriptions/[id]/page.tsx` respectively, holding all interactive state (list/filter/search/pagination on the list page; the current subscription value on the detail page) since `code-standards.md` requires Server Components by default and these need `useState`. No `Navbar` exists yet (same gap noted and deliberately deferred in feature 04's decisions — it needs `GET /users/me`, which no page fetches until later), so the detail page has a plain "← Back to Subscriptions" link instead of relying on a persistent nav to get back. `SubscriptionDetailClient` itself only renders read-only info (stat grid + `EditSubscriptionDialog`/`CancelSubscriptionDialog`/`DeleteSubscriptionDialog` trigger buttons + `PaymentHistoryList`) — editing happens entirely inside the Edit dialog, not inline on the page.

**Feature 07 — real data, both client-fetched:** both components now fetch through `lib/subscriptions.ts` instead of reading `lib/mock-data.ts` (deleted this feature — no remaining references). This had to happen entirely client-side, not in the Server Component `page.tsx` shells, because the access token lives in browser memory only (per `build-plan.md`/feature 04's decisions) and is never available during SSR — this was flagged as unresolved back in feature 04 and is now resolved by making `SubscriptionsPageClient`/`SubscriptionDetailClient` do their own `useEffect`-based fetch on mount, with `isLoading`/`error` states rendered in place of the table/detail body. `SubscriptionDetailClient` takes an `id: string` prop (not a pre-fetched `subscription`) and, on a `"Subscription not found"` API error specifically (matched against the exact string `SubscriptionsService`'s `findOwnedOrThrow` throws — brittle in theory, but it's a literal this codebase owns on both ends), calls `next/navigation`'s `notFound()` to render the framework's 404 boundary; verified working from a Client Component via Playwright (navigating to a random UUID renders the real not-found page), which isn't spelled out either way in the bundled Next 16 docs.

### Page metadata — `apps/web/app/subscriptions/page.tsx`, `apps/web/app/subscriptions/[id]/page.tsx`

`/subscriptions` exports a static `metadata` object (`title: "Subscriptions | SubTrack"`), same pattern as the root layout's `metadata` export. `/subscriptions/[id]` (feature 07): since the subscription's name is no longer available server-side (see above), `generateMetadata` was dropped in favor of a static fallback title (`"Subscription - SubTrack"`), and `SubscriptionDetailClient` sets `document.title` directly in a `useEffect` once the real fetch resolves (`${subscription.name} - SubTrack`) — a pragmatic client-side workaround for the same "no server-side token" constraint, not the Metadata API, since there's no server-rendered data to build metadata from.

### `StatCard`, `DashboardStats` — `apps/web/components/dashboard/{StatCard,DashboardStats}.tsx`

`StatCard` is a plain shadcn `Card`/`CardContent` wrapper — stat number (`text-[30px] leading-9 font-semibold text-text-primary`, matching `ui-tokens.md`'s Typography table exactly, no Tailwind size utility hits 30px/36px so it's a bracket value) above a muted label (`text-xs text-text-muted`). `DashboardStats` derives the 4 stat values straight from a `DashboardSummary` — `activeSubscriptionsCount` and `upcomingRenewals.length` come directly off the API shape, no client-side counting. Grid is `grid-cols-2 lg:grid-cols-4` (2×2 on narrow viewports, 4-across at `lg`, per `ui-rules.md`'s Dashboard Stat Cards section — the "mobile: 2x2" split in that rule is about the native mobile app in Phase 6, not a web breakpoint, but the same 2-column fallback reads fine on a narrow web viewport too).

### `CategoryBreakdownChart` — `apps/web/components/dashboard/CategoryBreakdownChart.tsx`

Client component (`"use client"`, required — Recharts renders via browser APIs/hooks and cannot run in a Server Component). Installed `recharts@^3.10.0` (approved in `code-standards.md`'s Dependencies list) — **note for future chart work:** this major version's `Cell` component is deprecated to a no-op stub (`(_props) => null`, confirmed via the context7 MCP server against the actual published v3.10.0, not assumed from older docs/training data) in favor of either a `shape` render-prop or, more simply, a `fill` field set directly on each datum in the `data` array (Recharts' `Pie`/`Bar` default shapes read `entry.fill` if present — the `LabelLine` source itself defaults its stroke to `entry.fill`, confirming this still works). Used the latter: `data.map(entry => ({ ...entry, fill: CATEGORY_CHART_COLORS[entry.category] }))`, `CATEGORY_CHART_COLORS` (new export in `lib/subscription-options.ts`, sits next to `CATEGORY_BADGE_CLASSES`) maps each category to its `var(--color-category-*)` CSS variable per `ui-rules.md`'s "chart colors always pulled from CSS variables, never hardcoded hex" rule. Donut via `<Pie innerRadius={55} outerRadius={80}>` inside `<ResponsiveContainer>` — no Recharts `<Legend>`; the legend is a hand-rolled `<ul>` below the chart (color swatch + category label + percentage) per `ui-rules.md`'s exact "legend below with category name + percentage" spec, since Recharts' built-in legend doesn't format percentages without extra plumbing. Self-guards its own empty case (`data.length === 0` → muted text, no chart) even though the page-level `DashboardEmptyState` already covers the only real-world zero-subscriptions case — kept for defensiveness/reuse, matches `SpendTrendChart`'s same self-contained guard.

### `SpendTrendChart` — `apps/web/components/dashboard/SpendTrendChart.tsx`

Client component, same Recharts-requires-client-boundary reasoning as `CategoryBreakdownChart`. Built as `<AreaChart>`/`<Area>` (not the bare `<LineChart>`/`<Line>` shown in `library-docs.md`'s minimal example) because `ui-rules.md` additionally requires "gradient fill beneath" the line — `ui-tokens.md`'s exact spec (`#0F9D78` stroke, 3px width, `rgba(15,157,120,0.15)` gradient fill) is only achievable with an `Area` (a `<defs><linearGradient>` from `var(--color-accent)` at 15% opacity down to 0%, referenced via `fill="url(#spendTrendFill)"`) — visually identical to a line chart since `dot={false}` and no area chart affordances (stacking, multiple series) are used. `XAxis` ticks run through the new `formatMonthLabel()` (`lib/format.ts`) to turn `"2026-02"` into `"Feb"`; `YAxis` ticks use `Intl.NumberFormat(..., { notation: "compact" })` inline (e.g. `145.8K`) since raw RWF totals are 6+ digits and would crowd the axis otherwise — not pulled into `lib/format.ts` since it's chart-axis-specific, not a general-purpose formatter like `formatCurrency`. Empty-state guard checks `points.every(p => p.amount === 0)`, not just `points.length === 0` — the real API (feature 10) always returns a full `months`-length array even with zero payment history (zero-filled points, not omitted), so an all-zero array is the actual shape this needs to detect per `ui-rules.md`'s "never render an empty/zeroed chart" rule. Verified live via Playwright with a temporarily-zeroed mock dataset (reverted after) that this exact scenario — confirmed for real against the live API in feature 10's own verification, where `payment_history` was genuinely empty — renders the muted-text empty state, not a flat zeroed chart.

### `UpcomingRenewalsList` — `apps/web/components/dashboard/UpcomingRenewalsList.tsx`

Plain Server Component list (no client state needed). Reuses `RenewalUrgencyPill` unchanged (already built for the subscription detail page in feature 06/07, matches `ui-rules.md`'s stated scope of "upcoming renewals list and subscription detail" for that component). Row: name + `formatCurrency(amount, currency)` (original currency, per `api-contract.md` — `upcomingRenewals` amounts are deliberately not converted) + urgency pill. Empty state: muted centered text, no CTA (nothing actionable — a subscription becoming due isn't a user action).

### `DashboardEmptyState` — `apps/web/components/dashboard/DashboardEmptyState.tsx`

Page-level empty state (distinct from the two chart-level empty states above) — renders instead of the entire stat-cards/charts/renewals body when `summary.activeSubscriptionsCount === 0`, per `build-plan.md`'s feature 11 spec. `lucide-react` `Inbox` icon + muted text + a Primary-styled CTA `Link` (`buttonVariants()` + the same `bg-accent text-accent-foreground hover:bg-accent-dark` override every other Primary action in this codebase uses) pointing at `/subscriptions` — there's no add-subscription dialog on the dashboard itself, so the CTA sends the user to where one exists rather than duplicating that flow. `Link` styled via `buttonVariants` instead of wrapping `Button` (which has no `asChild` support, unlike Radix-based shadcn `Button`s — this project's `Button` is Base UI) — same `cn(buttonVariants(), ...)` pattern, no new convention introduced.

### `apps/web/lib/mock-dashboard-data.ts`

Mock-phase data source for `/dashboard` (feature 11 — no API wiring yet, that's feature 12), mirroring the real feature 10 API's actual verified output almost exactly (same totals, category split, and 3 upcoming renewals seen when the live endpoint was curled) rather than inventing arbitrary numbers, so the mock UI and the eventual real wiring look the same. Exports `mockDashboardSummary`/`mockSpendTrend` only — no separate exported "empty" variant shipped (unlike some earlier mock datasets), since the empty-state and zero-spend-trend branches were verified once via a temporary in-place edit + Playwright screenshot + revert, not left as a permanent alternate dataset (nothing in the codebase currently needs to switch between them at runtime).

### Page — `apps/web/app/dashboard/page.tsx`

Stays a plain Server Component (no `"use client"`, no `DashboardPageClient` wrapper) — unlike `/subscriptions`, the dashboard has zero interactive/mutable state in this mock-only feature (no forms, no dialogs, nothing to `useState`), so `code-standards.md`'s "Server Components by default" applies with no exception needed yet. Only the two chart leaf components are client boundaries. This will very likely need to change in feature 12 once real fetching (and the build plan's explicit "independent loading states per card/chart" requirement) is wired in — expect that feature to introduce client wrapper(s) at that point, not before.

### `Switch` — `apps/web/components/ui/switch.tsx`

shadcn CLI-generated (`npx shadcn add switch`), unmodified — resolves cleanly for this style like `Table`/`Dialog`/`Select`/`Badge`/`Pagination`/`AlertDialog`. Built on `@base-ui/react`'s `Switch` primitive: renders a `<span role="switch">` (not a `<button>`) plus a hidden native `<input>`. **Gotcha:** an `id` prop passed to `Switch` lands on the hidden native input (for label association via `htmlFor`), not on the visible `span[role="switch"]` — the visible span always gets its own internally-generated id. This is intentional Base UI behavior, not a bug: don't try to assert `id` equality against the visible switch element in tests: query by `[role="switch"]` and check `aria-checked`/`aria-disabled` instead (there is no real `disabled` DOM attribute — disabled state is `aria-disabled="true"` + `data-disabled`).

### `Navbar` — `apps/web/components/layout/Navbar.tsx`

New in feature 13 — closes the gap flagged and deliberately deferred since feature 04 ("no page fetches `GET /users/me` until later"), now that Settings does. Client component (`usePathname` for active-link state, `useEffect` fetch, logout handler). Exactly per `ui-rules.md`: 64px header (`h-16`), white surface, three nav items (Dashboard/Subscriptions/Settings) with active = `text-accent` / inactive = `text-text-secondary` (no underline), user name (14px/500/`text-text-primary`) + 32px `bg-accent-light`/`text-accent` initial-letter avatar circle on the right — fetched from `GET /users/me`, never hardcoded. **One addition beyond the literal spec:** a small "Log out" text button next to the avatar — `ui-rules.md` doesn't specify where logout lives, but `lib/auth.ts`'s `logout()` had zero callers anywhere in the app until this feature, so without it there was no way to sign out from the UI at all. Judgment call, not a spec deviation of anything that *was* specified. **Sync gotcha (fixed during this feature):** the navbar fetches its profile once on mount, but a persisted layout doesn't remount on client-side navigation — so saving a new name in Settings left the navbar showing the stale name until a full reload. Fixed with a small hand-rolled pub/sub: `lib/users.ts` exports `PROFILE_UPDATED_EVENT` and `updateCurrentUserProfile()` dispatches it on `window` after a successful `PATCH /users/me`; `Navbar` adds a listener alongside its mount-time fetch. No new dependency — same "hand-rolled over fighting the framework" precedent as `SnakeNamingStrategy`/the manual cookie parser.

### `(app)` route group — `apps/web/app/(app)/`

New in feature 13, to give `Navbar` a single place to render across `/dashboard`, `/subscriptions`, `/subscriptions/[id]`, and the new `/settings` without duplicating it per-page. `dashboard/`, `subscriptions/`, `subscriptions/[id]/` were moved here unchanged (route groups don't affect URL paths — confirmed via `next build`'s route table, still `/dashboard`, `/subscriptions`, `/subscriptions/[id]`); this codebase already had precedent for a route-group layout (`(auth)/layout.tsx` from feature 04). `app/(app)/layout.tsx` renders `<Navbar />` + `<main>{children}</main>` inside a `bg-background` flex column; each page's own `p-8`/`max-w-7xl` content wrapper is unchanged. **Windows file-lock note:** `git mv` on the whole `subscriptions/` directory failed with `EPERM`/"Permission denied" because the running `next dev` file watcher (Turbopack) held an open handle on it — worked around by moving the two `page.tsx` files individually (`git mv subscriptions/page.tsx "(app)/subscriptions/page.tsx"`, same for `[id]/page.tsx`) instead of renaming the parent directory, then `rmdir`-ing the emptied original folders. `dashboard/` moved fine as a whole directory (no such lock hit that one).

### `ProfileSection`, `SpendLimitSection`, `NotificationsSection`, `SettingsPageClient` — `apps/web/components/settings/*.tsx`

`ProfileSection`: `react-hook-form` + `zodResolver`, same shape as `SubscriptionForm`. Name + Base Currency (shadcn `Select`, reuses `CURRENCY_OPTIONS`) are real `FormField`s; **Email is deliberately NOT wrapped in `FormField`** — it's not part of the form's zod schema (read-only, never submitted), so it's a plain `Label` + disabled `Input` pair instead. First attempt used `FormItem`/`FormLabel`/`FormControl` (this project's hand-written primitives) without a `FormField` ancestor for the email row — crashed at runtime (`useFormField must be used within <FormField> and <FormItem>`, caught via Playwright's `pageerror` listener, not `tsc`/`eslint`, since it's a React-context runtime error) because `FormLabel`/`FormControl` both call `useFormField()`, which requires the `FormFieldContext` that only `FormField` (react-hook-form's `Controller` wrapper) provides — `FormItem` alone only provides the sibling `FormItemContext`. Any future read-only field placed inside a `Form` must use plain `Label`/`Input`, not the `Form*` primitives, unless it's wired to an actual `FormField`. Save button disabled via `form.formState.isSubmitting || !form.formState.isDirty` (no point re-submitting unchanged values); on success, calls the new `updateCurrentUserProfile()` (`lib/users.ts`) and `form.reset(values)` to clear the dirty flag against the new baseline. `NotificationsSection` is still the v2-scaffolded section per `ui-rules.md`: a plain (non-`"use client"`, no hooks) `Card` with a `CardHeader` holding the title + a `Badge` (`bg-info-light text-info-foreground`, "Coming in v2") inline via `className="flex flex-row items-center gap-2"` overriding `CardHeader`'s default `grid`, and two disabled `Switch` rows — "Renewal Reminders"/"Spend Limit Alerts", named directly after `notification_preferences`'s two boolean columns in `architecture.md` (`renewalRemindersEnabled`/`spendLimitAlertsEnabled`) since no other spec names them. Doesn't wire to any endpoint yet — matches `build-plan.md`'s "disabled v2 sections render but submit nothing." **`SpendLimitSection` was un-scaffolded in feature 19** (build-plan Phase 7) — the `Badge`/disabled-`Input` pair is gone; it's now a real numeric `Input` (`type="number" min="0" step="0.01"`) + `Label` ("Limit ({baseCurrency})") + `Save Changes` `Button`, `"use client"` with local `useState` seeded from `profile.monthlySpendLimit` (already returned by the real `GET /users/me` since feature 18 — reading it isn't "mock", only the *save* path is: `onSaved` just merges the parsed value into the parent's `UserProfile` state, no `PATCH` call yet, mirroring feature 06's "Add/Edit forms update local component state only" precedent for mock-phase UI). Save is disabled when unchanged (string comparison against the seeded value) or when the typed value is negative (inline `text-error` message, pre-empting the API's `@Min(0)` rule from feature 18 rather than waiting for a round-trip). `SettingsPageClient` passes `profile`/`onSaved={setProfile}` to `SpendLimitSection` the same way it already does for `ProfileSection`, so a mock save is reflected immediately across both cards without a reload. `SettingsPageClient` itself is otherwise unchanged: thin-Server-Component-page + Client-wrapper shape shared with `DashboardPageClient`/`SubscriptionsPageClient`, one `useEffect` fetch of `GET /users/me` with `isLoading`/`error` state.

### `Progress`, `ProgressTrack`, `ProgressIndicator`, `ProgressLabel`, `ProgressValue` — `apps/web/components/ui/progress.tsx`

Added in feature 19 (first use of any progress bar in this codebase) via `npx shadcn add progress` — resolves cleanly for this project's `base-vega` style, built on `@base-ui/react/progress` (same primitive family as `Switch`, not Radix). **Gotcha, fixed in the generated file itself:** the CLI's default `Progress` root unconditionally renders `{children}` *and then* its own default `<ProgressTrack><ProgressIndicator /></ProgressTrack>` after them — passing a custom colored `ProgressTrack`/`ProgressIndicator` as `children` (the only way to override per-instance fill color, since there's no `indicatorClassName` prop) would render two overlapping tracks. Changed `{children}<ProgressTrack>...` to `{children ?? <ProgressTrack>...}` so custom children fully replace the default instead of appending to it — same class of justified generated-file edit as `Input`/`Form`. Default `ProgressTrack`/`ProgressIndicator` still read `bg-muted`/`bg-primary` (shadcn's semantic vars, already mapped to real SubTrack tokens in `globals.css`) when no override is given; `SpendLimitProgress` always overrides both explicitly with real tokens (`bg-surface-secondary` track, `bg-accent`/`bg-warning`/`bg-error` indicator) per `ui-tokens.md`'s "project tokens only" rule.

### `SpendLimitProgress` — `apps/web/components/dashboard/SpendLimitProgress.tsx`

New in feature 19, rendered in `DashboardPageClient` directly under `DashboardStats`. Takes `spendLimit`/`currentMonthSpend`/`percentageUsed`/`isOverLimit`/`baseCurrency` as props — the exact shape `GET /dashboard/summary` gained in feature 18, but **fed from a new mock file, `apps/web/lib/mock-spend-limit.ts`** (`baseCurrency` alone comes from the real, already-wired `summary.baseCurrency`), per `build-plan.md`'s "built against mock data first" instruction for this feature even though the real endpoint already exists — feature 20 wires the remaining four fields and deletes the mock file, same lifecycle as `mock-dashboard-data.ts` before it. Three visual states per `build-plan.md`: `percentageUsed === null` (no limit set) renders a CTA card ("Set a monthly limit to track your spending against it." + a button linking to `/settings`) instead of a bar — never a zeroed/fake one; otherwise a `Progress` bar clamped to 100% width (`Math.min(percentageUsed, 100)` — a real `percentageUsed` can exceed 100 when over limit) colored `bg-accent` under 90%, `bg-warning` at 90–100%, `bg-error` when `isOverLimit`, with a `{current} / {limit}` header line (same color as the bar) and a `"{percentageUsed}% used"` caption (` — over limit` appended when applicable). All four states (under-limit, warning, over-limit, no-limit CTA) verified via Playwright screenshots against a live login session, temporarily editing `mock-spend-limit.ts`'s values and reverting to the shipped under-limit default afterward — same "temporary edit + screenshot + revert" pattern feature 11 used for the dashboard's empty state, rather than keeping multiple mock variants live in the file.

### `DetectedSubscriptionsBanner`, `DetectedSubscriptionsPageClient`, `DetectedSubscriptionRow` — `apps/web/components/subscriptions/{DetectedSubscriptionsBanner,DetectedSubscriptionsPageClient,DetectedSubscriptionRow}.tsx`

Built in feature 29, wired to the real API in feature 30. Chose `build-plan.md`'s "dedicated `/subscriptions/detected`" option over inlining the list on `/subscriptions` itself — `ui-rules.md`'s navbar is fixed at exactly 3 items (Dashboard/Subscriptions/Settings), so the new route isn't nav-linked; discoverability instead comes from `DetectedSubscriptionsBanner`, a plain white `Card` (**never** `bg-accent-light` — `ui-rules.md`'s "never use colored card backgrounds" rule applies to every `Card`, not just the subscriptions table) rendered above the subscriptions table with a `MailSearch` icon, the pending count, and a Primary "Review" link to the new route; renders nothing when the pending count is 0. `SubscriptionsPageClient` fetches the count itself via `listDetectedSubscriptions()` (`lib/detected-subscriptions.ts`) in its own independent `useEffect` — not tied to the subscriptions-list `refreshToken`, since approving/dismissing a detected item happens on a different page and this count only needs to be fresh on this page's own mount. `DetectedSubscriptionsPageClient` fetches the same endpoint into `pending` state (loading/error states matching `SubscriptionsPageClient`'s own conventions), removing an item from local state on a successful approve or dismiss rather than refetching the whole list. `DetectedSubscriptionRow` shows vendor name + a billing-cycle `Badge` (or an amber "Cycle unknown" badge when the parser couldn't detect one — `billingCycle`/`amount`/`currency` are all nullable on the `detected_subscriptions` entity, since Gmail parsing per feature 28 is best-effort), the raw email subject, formatted amount (`formatCurrency`, "Amount not detected" fallback) and received date. **Reused `formatDate` for `receivedAt`** rather than adding a new formatter — `receivedAt` is a full ISO timestamp (`timestamptz` on the entity) but `formatDate` expects a plain `YYYY-MM-DD` string, so the row does `detected.receivedAt.slice(0, 10)` before calling it, avoiding a near-duplicate `formatDateTime` util for one field. Dismiss is styled Secondary (`border-accent bg-surface text-accent hover:bg-accent-light`, **no** `variant="outline"`) — now a real, self-contained async action (local `isDismissing`/`error` state, calls `dismissDetectedSubscription` directly, no confirmation dialog: lower-stakes than cancelling a real subscription, and the mock-phase UX was already a single click). Approve is no longer a single click — it opens `ApproveDetectedSubscriptionDialog` (Primary trigger button). Empty state (all items reviewed): `MailSearch` icon + muted text + a Secondary-styled link back to `/settings` to manage the Gmail connection.

### `ApproveDetectedSubscriptionDialog` — `apps/web/components/subscriptions/ApproveDetectedSubscriptionDialog.tsx`

New in feature 30. `detected_subscriptions` has no `category` or `startDate` column (not detectable from an email) and `vendorName`/`amount`/`currency`/`billingCycle` are all nullable best-effort parses — so approving can't be a single click against the real API the way the feature-29 mock version was. Reuses `SubscriptionForm` exactly like `AddSubscriptionDialog`/`EditSubscriptionDialog` do, `defaultValues` prefilled from the detected item (`name: vendorName ?? ""`, `cost: amount ?? 0`, `currency: currency ?? "USD"`, `billingCycle: billingCycle ?? "monthly"`, `startDate: receivedAt?.slice(0, 10) ?? today`) with `category` left at the form's own default (`"entertainment"`) since there's no detected-data equivalent to prefill it from — the user picks a real one before submitting. Submits to `approveDetectedSubscription(id, values)` (`lib/detected-subscriptions.ts`), which hits `POST /integrations/detected/:id/approve`; on success calls `onApproved(id, subscription)` so the parent row list can remove the item, same success-callback pattern as `EditSubscriptionDialog`'s `onSaved`.

### `GmailConnectionSection` — `apps/web/components/settings/GmailConnectionSection.tsx`

Built in feature 29 as a local-state-only toggle, wired to the real API in feature 30 (`lib/integrations.ts`: `getGmailStatus`, `getGmailConnectUrl`, `disconnectGmail`). Fetches connection status via `GET /integrations/gmail/status` on mount (a new feature-30 endpoint — feature 27 shipped connect/callback/disconnect only and explicitly deferred a status/read endpoint to whichever feature actually needed one, per that feature's progress-tracker note). "Connect Gmail" calls `GET /integrations/gmail/connect` (authenticated, returns the consent URL as JSON) then does a plain `window.location.href = url` — matching `api-contract.md`'s explicit note that the actual navigation to Google is a browser redirect, not a second `fetch()`. Shows a `Mail` icon, connected/not-connected/"Checking connection…" status text, and a Primary "Connect Gmail" or outlined-destructive "Disconnect" button (`border-error bg-surface text-error hover:bg-error-light`, no `variant="outline"`), both disabled while their own request is in flight, with an inline `text-error` message on failure — no connected-email address is shown, since `email_connections` doesn't store one.

### Settings' `gmail=connected`/`gmail=error` banner — `apps/web/app/(app)/settings/page.tsx`, `apps/web/components/settings/SettingsPageClient.tsx`

New in feature 30. `GET /integrations/gmail/callback` (feature 27) always ends in a `302` to `/settings?gmail=connected|error` — `settings/page.tsx` is an `async` Server Component that reads the Page `searchParams` prop (a `Promise` in this Next.js version — see `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md`) and passes a narrowed `gmailStatus: "connected" | "error" | null` down to `SettingsPageClient`, rather than `useSearchParams()` in a Client Component (which would need a `Suspense` boundary for the production build per that hook's own docs — the `searchParams` prop is the simpler option when already in a Server Component page). `SettingsPageClient` renders a tinted inline banner (`border-accent bg-accent-light text-accent` / `border-error bg-error-light text-error`) above the section cards when `gmailStatus` is set — no toast library added (none is on `code-standards.md`'s approved dependency list and this is a one-off, load-time-only message; a plain banner div needed no new dependency).

### `apps/web/lib/detected-subscriptions.ts`, `apps/web/lib/integrations.ts`

New in feature 30, replacing `apps/web/lib/mock-detected-subscriptions.ts` (deleted — same mock-file lifecycle as `mock-dashboard-data.ts`/`mock-spend-limit.ts` before it). `lib/detected-subscriptions.ts`: `listDetectedSubscriptions`, `approveDetectedSubscription`, `dismissDetectedSubscription` against `/integrations/detected*`. `lib/integrations.ts`: `getGmailStatus`, `getGmailConnectUrl`, `disconnectGmail` against `/integrations/gmail/*`. Split into two files rather than one, mirroring the API's own module split (Gmail connection vs. detected-subscription review are separate concerns sharing the `integrations` domain).

### `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuGroup`, `DropdownMenuLabel`, `DropdownMenuItem`, `DropdownMenuSeparator` — `apps/web/components/ui/dropdown-menu.tsx`

New in feature 32. shadcn CLI-generated (`npx shadcn add dropdown-menu`), unmodified — resolves cleanly for this style (`base-vega`) like every other CLI-generated component so far. Built on `@base-ui/react/menu`'s `Menu` primitive (no new dependency — `@base-ui/react` was already installed). `DropdownMenuTrigger` renders as a real `<button>` with no default styling, so it's made to look like a `Button` the same way `DetectedSubscriptionsBanner`'s `Link` does — `cn(buttonVariants(), ...)` directly on the trigger element — rather than wrapping the actual `Button` component, consistent with this project's established "no `asChild`" workaround (`Button` is Base UI, not Radix).

### `apps/web/lib/export.ts`

New in feature 32. `downloadExport(resource, format)` against `GET /export/subscriptions` / `GET /export/payment-history` — the first client calls in this codebase whose *success* response isn't `apiFetch`'s `{success,data}` JSON envelope (`api-contract.md`'s Export section documents this explicitly: a raw file stream on success, the normal envelope only on error). Bypasses `lib/api-client.ts`'s `apiFetch` entirely with its own `fetchExport`, since `apiFetch` always calls `res.json()`, which would throw on a real CSV/PDF body — but duplicates its 401-then-refresh-then-retry logic (`refreshAccessToken()` from `lib/auth.ts`, redirect to `/login` on a failed refresh) so the export flow shares the same session-expiry behavior as every other API call. On success, reads the real filename off the `Content-Disposition` response header (regex on `filename="..."`) and triggers the download via a temporary `URL.createObjectURL(blob)` + `<a download>` click — no file-saver dependency needed for this one-off. **Gotcha, fixed during this feature:** `Content-Disposition` isn't one of the CORS-safelisted response headers browsers expose to `fetch()` by default, so `res.headers.get("Content-Disposition")` silently returned `null` (filenames fell back to `subscriptions-export.csv`, missing the date `api-contract.md` requires) until `apps/api/src/main.ts`'s `app.enableCors(...)` call gained `exposedHeaders: ['Content-Disposition']` — verified live via Playwright before and after the fix (before: `subscriptions-export.csv`; after: `subscriptions-export-2026-07-24.csv`, matching the contract exactly).

### `ExportMenu` — `apps/web/components/subscriptions/ExportMenu.tsx`

New in feature 32, rendered in `SubscriptionsPageClient`'s header row next to `AddSubscriptionDialog`. `build-plan.md`'s feature 32 spec only names an "Export menu/button on `/subscriptions`" for the subscriptions export, but says nothing about where the payment-history export (the other endpoint feature 31 shipped) surfaces in the UI — rather than leave that second endpoint permanently unreachable from any page, this one menu covers both: a `DropdownMenu` with two `DropdownMenuGroup`s ("Subscriptions" / "Payment History", separated by a `DropdownMenuSeparator`), each with "Export as CSV" / "Export as PDF" items, four `downloadExport()` calls total. Trigger styled Secondary (`border-accent bg-surface text-accent hover:bg-accent-light`, a `Download` `lucide-react` icon), matching `EditSubscriptionDialog`'s reasoning for Secondary over Primary (a real action, but not the page's main one). All four menu items disable together during any single in-flight export (one `pendingKey` state, not per-item) to avoid overlapping downloads; the pending item's own label swaps to "Exporting…", same `isDismissing`-style convention as `DetectedSubscriptionRow`'s Dismiss button. On failure, an inline `role="alert" text-xs text-error` message renders under the button — no toast library, same reasoning as feature 30's Gmail-connect banner (nothing on `code-standards.md`'s approved dependency list, and this is a one-off message).

---

## Mobile Components

### `Input` — `apps/mobile/components/ui/Input.tsx`

`forwardRef<TextInput>`, label + `TextInput` + optional error, matching `ui-rules.md`'s Form Input spec (`bg-surface border border-border rounded-md px-3 py-2`, focus → `border-accent`, error → `border-error`). Focus state is local `useState` toggled via `onFocus`/`onBlur` rather than a NativeWind `focus:` variant — not verified to work reliably on `TextInput` in this environment (no simulator available to confirm), so handled the safe, definitely-supported way instead. `placeholderTextColor` is a native `TextInput` prop (can't take a Tailwind class), sourced from the new `constants/colors.ts` rather than a hardcoded hex — see that file's note.

### `PasswordInput` — `apps/mobile/components/ui/PasswordInput.tsx`

Wraps `Input` with a `secureTextEntry` toggle. Show/hide control is a plain text `Pressable` ("Show"/"Hide"), not an icon button — no icon library is an approved mobile dependency (`@expo/vector-icons` ships with Expo but wasn't added to avoid an unreviewed new UI dependency for one button; revisit if a later feature needs icons broadly). Absolutely positioned inside the wrapper at `right-3 top-9` — that offset assumes the `Input` label + `gap-1` layout and hasn't been visually confirmed on-device (see the feature 14 progress-tracker note on unverified rendering).

### `Button` — `apps/mobile/components/ui/Button.tsx`

`Pressable` + `Text`, Primary style only (`bg-accent`, `text-accent-foreground`, `rounded-md`, `px-4 py-2`) — no Secondary/Destructive variants yet, since nothing built so far needs them (the Sign Out button on the placeholder Settings screen passes `className="bg-error"` as a one-off override rather than a real variant prop). Add real variants when a feature needs one instead of guessing the API shape now. `loading` prop shows an `ActivityIndicator` (color from `constants/colors.ts`) before the label and disables the press target, same disabled-state handling as the plain `disabled` prop.

### `constants/colors.ts` — `apps/mobile/constants/colors.ts`

Plain-object mirror of `tailwind.config.js`'s color tokens, camelCased, for native props that can't take a NativeWind `className` (`placeholderTextColor`, `ActivityIndicator`'s `color`, and — per `library-docs.md` — `react-native-chart-kit`'s `chartConfig`/`legendFontColor` later). Replaces the deleted stock-template `constants/theme.ts` (light/dark `Colors` object) — this app has one theme only, per `ui-tokens.md`. Must stay in sync with `tailwind.config.js` and `ui-tokens.md`'s Mobile Token Mirror.

### `(auth)/login.tsx`, `(auth)/register.tsx` — `apps/mobile/app/(auth)/*.tsx`

Self-contained screens (no `components/auth/` folder — `architecture.md`'s mobile component tree only lists `dashboard/`, `subscriptions/`, `ui/`, so form logic lives directly in the screen per `code-standards.md`'s "screens call `lib/api-client.ts` and render"). Plain `useState` + `zod`'s `safeParse` on submit (no `react-hook-form` — it's not an approved mobile dependency, unlike web), mapping `ZodError.issues[].path[0]` to a `FieldErrors` record for inline messages. Branded header (36×36 `bg-accent` box + "S" + "SubTrack" wordmark) is a flat `bg-accent` fill, not web's gradient — `expo-linear-gradient` isn't an approved dependency and plain NativeWind doesn't render CSS gradients on native views. Calls `useSession().signIn`/`signUp` (`lib/auth-context.tsx`) — no manual `router.replace` on success; `Stack.Protected` in the root layout reacts to `isAuthenticated` and swaps the whole stack.

### `(tabs)/_layout.tsx`, `dashboard.tsx`, `subscriptions.tsx`, `settings.tsx` — `apps/mobile/app/(tabs)/*.tsx`

Minimal 3-tab shell (`expo-router`'s `Tabs`, no icons) built only so the auth guard has a real, testable authenticated destination — feature 14's scope is auth, not these screens' real content. Each body is a centered "Coming soon" placeholder. The full spec (4 tabs incl. the prominent center "Add" button, per `ui-rules.md`'s Navigation section) is deferred to feature 15, since "Add" has nowhere to navigate to until then. `settings.tsx` additionally renders a Sign Out button (`useSession().signOut()`) — not speced anywhere yet (same gap web hit until its feature 13 Navbar), included now because the auth guard needs a real way to flip back to unauthenticated for testing; will move/merge into the real Settings screen in feature 17.

### `app/_layout.tsx`, `lib/auth-context.tsx` — `apps/mobile/app/_layout.tsx`, `apps/mobile/lib/auth-context.tsx`

Root layout loads Inter (4 weights via `@expo-google-fonts/inter`'s `useFonts`) and holds `SplashScreen.preventAutoHideAsync()` until **both** fonts are loaded and `SessionProvider`'s cold-start session bootstrap resolves — the latter is a deliberate extension beyond the stock font-only splash pattern, needed to avoid a login-screen flash for an already-logged-in user. Uses `expo-router`'s current (SDK 57) `Stack.Protected` pattern — `guard={isAuthenticated}` / `guard={!isAuthenticated}` wrapping `(tabs)` / `(auth)` respectively — confirmed via the context7 MCP server against `docs.expo.dev`'s `router/advanced/authentication` guide as the current recommended approach, rather than the older manual `<Redirect>`-in-layout pattern. `SessionProvider` bootstraps by reading a persisted refresh token from `expo-secure-store` and attempting one silent refresh; on a fresh install (no stored token) or a failed refresh, `isAuthenticated` stays `false`. `lib/auth.ts` (pure token functions) and `lib/auth-context.tsx` (the React Context) are split — `architecture.md`'s tree only lists one `lib/auth.ts`, but mobile additionally needs a reactive hook for `Stack.Protected`'s `guard`, which web's cookie-based redirect model never required.