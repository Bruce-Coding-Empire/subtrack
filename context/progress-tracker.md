# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 6 — Mobile App
**Last completed:** 17 Mobile Settings
**Next:** None — all 17 build-plan features complete. See `build-plan.md`'s v2 Roadmap for what's next.

---

## Progress

### Phase 1 — Foundation

- [x] 01 Monorepo + Tooling Setup
- [x] 02 Database Schema + Migrations
- [x] 03 Auth — API
- [x] 04 Auth — Web UI + Wiring

### Phase 2 — Subscriptions Core

- [x] 05 Subscriptions API
- [x] 06 Subscriptions Page — Web UI (mock data)
- [x] 07 Subscriptions — Web Wiring

### Phase 3 — Renewals, History & Currency

- [x] 08 Renewal Scheduled Job
- [x] 09 Currency Module + Exchange Rate Job

### Phase 4 — Dashboard

- [x] 10 Dashboard API
- [x] 11 Dashboard Page — Web UI (mock data)
- [x] 12 Dashboard — Web Wiring

### Phase 5 — Settings

- [x] 13 Settings Page — Web (Full, incl. v2 scaffolding)

### Phase 6 — Mobile App

- [x] 14 Mobile Auth
- [x] 15 Mobile Subscriptions
- [x] 16 Mobile Dashboard
- [x] 17 Mobile Settings

---

## Decisions Made During Build

- `context/git-workflow.md` was missing on session start despite being required reading in `AGENTS.md`; it has since been added.
- `apps/mobile` had already been scaffolded via `create-expo-app`'s default "src directory" template (`src/app`, `src/components`, etc.), which contradicted the flat `app/`, `components/`, `lib/` layout documented in `architecture.md`. Flattened it to match `architecture.md` exactly (moved `src/*` to the app root, updated the `@/*` path alias in `tsconfig.json`). Deleted the stock `scripts/reset-project.js` and its `package.json` script since it hardcoded the old `src/` paths and doesn't apply to this project.
- NativeWind pinned to `nativewind@^4.2.6` with `tailwindcss@^3.4.0` in `apps/mobile` (NativeWind v4 requires Tailwind v3's config-based API, not v4's `@theme` syntax — matches the distinction already called out in `ui-rules.md`). `apps/web` keeps `tailwindcss@^4`; npm workspaces nests a separate `tailwindcss@4.3.3` under `apps/web/node_modules` to keep the two versions from colliding at the hoisted root.
- Added `expo-env.d.ts` and `nativewind-env.d.ts` to `apps/mobile` — Expo/NativeWind normally generate these on first `expo start`/`expo install` run; created manually since the app had never been run. Both are gitignored, consistent with being generated files.
- `npx expo export --platform web` fails on this monorepo layout with `Cannot find module 'expo-router/build/utils/url'` — a pre-existing hoisting issue where `expo-router` resolves to a nested `apps/mobile/node_modules` copy that `@expo/cli`'s static-export step can't reach via plain Node `require`. Metro itself bundles the app successfully (verified: 1226 modules, web platform), and static web export isn't in v1 scope for mobile (native app via Expo Go/simulators), so this was left unresolved. Revisit if a future feature needs `expo export --platform web`.
- `apps/api/tsconfig.json` had `baseUrl` but no `paths` entry, so `code-standards.md`'s mandated `@/` import alias had nothing to resolve against for the API. Fixed as `"paths": { "@/*": ["./src/*"] }` with **no `baseUrl`** — `baseUrl` is deprecated (removed in TS 7.0) and TS 5.9.3 errors on the `"ignoreDeprecations": "6.0"` value the deprecation message itself suggests, so the correct fix is to drop `baseUrl` entirely rather than silence the warning. Since TS 4.1, `paths` resolves relative to the tsconfig file's own location when `baseUrl` is unset (path values must then start with `./`) — this matches the pattern `apps/web/tsconfig.json` already uses. Verified empirically (not assumed) that the Nest CLI's own compiler — used by both `nest build` and `nest start --watch` — already rewrites `@/...` imports to correct relative `require()` paths in the emitted JS, so no extra runtime tooling (`tsc-alias`, `tsconfig-paths/register`) is needed; a first attempt to add `tsc-alias` as a belt-and-suspenders measure was reverted once testing showed it was redundant. Plain `tsc` (not routed through `nest build`) does *not* do this rewrite — don't reach for raw `tsc` for anything that needs `@/` imports in this app.
- `npm install typeorm` pulled **`typeorm@1.1.0`** — TypeORM has since graduated off its long-standing 0.3.x pre-1.0 line to a stable 1.x major (confirmed via `npm view typeorm version`, not assumed). All patterns in `library-docs.md` (entity decorators, `DataSource`, migration CLI) still work unchanged against 1.1.0; noting the version jump so a future session isn't surprised by `"typeorm": "^1.1.0"` in `package.json`.
- Added a hand-written `SnakeNamingStrategy` at `apps/api/src/common/utils/snake-naming.strategy.ts` (extends TypeORM's `DefaultNamingStrategy`, no new dependency) so entity properties stay camelCase (per `code-standards.md`/`library-docs.md` entity examples) while actual Postgres columns are snake_case (per `architecture.md`'s documented schema, e.g. `next_renewal_date`, `password_hash`). Registered in both `app.module.ts`'s `TypeOrmModule.forRootAsync` and the CLI-only `database/data-source.ts`.
- `architecture.md`'s monorepo tree has no dedicated `notifications` module, so the v2-scaffolded `notification_preferences` entity was placed at `modules/users/entities/notification-preference.entity.ts` (user-owned data, not wired into any service/controller — table exists per the build plan, nothing reads/writes it in v1).
- `exchange_rates`'s documented unique constraint — `(base_currency, target_currency, fetched_at::date)` — is a Postgres *expression* index that TypeORM entity decorators can't express directly. Generated the migration from entities first, then hand-added `CREATE UNIQUE INDEX ... (("fetched_at")::date)` (and its `DROP INDEX` in `down()`) directly in the migration file. **Flag for feature 09**: `library-docs.md`'s example upsert (`currency.service.ts`) uses a two-column conflict target (`["baseCurrency", "targetCurrency"]`), which won't match this three-column unique index — Postgres `ON CONFLICT` requires an exact match to a real unique constraint/index. Reconcile this when building the Currency Module (either upsert conflict target must become 3 columns using today's date, or the constraint should drop the date component — revisit then, not decided now).
- The TypeORM CLI (`typeorm-ts-node-commonjs`, used for `migration:generate`/`migration:run`/`migration:revert`) runs on raw `ts-node`, which — unlike `nest build`/`nest start` — does **not** rewrite `@/` path aliases on its own. Fixed by adding a `"ts-node": { "require": ["tsconfig-paths/register"] }` block to `apps/api/tsconfig.json`; `ts-node` auto-reads this block, so no extra CLI flags or `NODE_OPTIONS` juggling needed, and it's cross-platform (matters since this session runs both PowerShell and Git Bash).
- TypeORM's default `uuid` primary key strategy emits `DEFAULT uuid_generate_v4()`, which requires the `uuid-ossp` Postgres extension. Not enabled by default on a fresh database, so `CREATE EXTENSION IF NOT EXISTS "uuid-ossp"` was added as the first statement in the initial migration's `up()`.
- Local Postgres runs as an already-installed Windows service (`postgresql-x64-18`, verified via `Get-Service`), not Docker — no `docker-compose.yml` needed for this project. The `subtrack` database didn't exist yet and was created manually via `psql -U postgres -c "CREATE DATABASE subtrack;"` before running migrations.
- Dev seed script (`apps/api/src/database/seed.ts`, run via `npm run seed`) creates one test user (`test@subtrack.dev` / `Password123!`, bcrypt-hashed at 10 rounds per `library-docs.md`) and 7 subscriptions spanning all 5 categories and 3 currencies (USD, RWF, EUR), covering weekly/monthly/yearly/custom-adjacent cycles. `start_date`/`next_renewal_date` are hardcoded fixture values rather than computed — `billing-cycle.util.ts` doesn't exist until feature 05, and this is one-off seed data rather than a runtime calculation path, so the "`next_renewal_date` only via `billing-cycle.util.ts`" invariant (which governs application code) doesn't apply here. Script is idempotent — deletes any existing seeded user (and their subscriptions) by email before inserting, safe to re-run.
- Installed `bcrypt` (+ `@types/bcrypt`) now rather than deferring to feature 03, since the seed script needed a real password hash for the test user to be usable once Auth API lands. Already on the approved dependency list in `code-standards.md`.
- **03 Auth — API.** Installed `@nestjs/jwt`, `@nestjs/passport`, `passport`, `passport-jwt` (+ `@types/passport-jwt`) — all on the approved list except bare `passport`, which is an unavoidable peer dependency of the other two, not a discretionary addition. Also installed `@nestjs/swagger` here (moved up from its own placeholder — see below) since feature 03 is where the build plan says the Swagger bootstrap and first decorated controller happen together.
- `api-contract.md`'s documented register/login response was missing `refreshToken` in the body, but `library-docs.md`'s Nest Passport + JWT rules explicitly require it there for mobile (no cookies to rely on). Updated `api-contract.md` first (per its own "update the contract file first" rule) to include `refreshToken` in the response `data`, with a note that web ignores the body copy and relies on the httpOnly cookie instead — mobile's `expo-secure-store` persistence (feature 14) is the only consumer of that field.
- Skipped the `cookie-parser` package for reading the refresh token off the web request in `POST /auth/refresh` — it's not on the approved dependency list in `code-standards.md`, and Express's `res.cookie()` (used to *set* the httpOnly cookie) doesn't require it. Wrote a ~10-line manual parser (`common/utils/cookie.util.ts`) instead, consistent with this project's existing preference for hand-rolled solutions over new dependencies for trivial needs (see the `SnakeNamingStrategy` decision above).
- `main.ts`'s Swagger setup (added in an earlier session, ahead of this feature) used path `docs` with no production gate. Corrected to match `library-docs.md` exactly: mounted at `api/docs`, wrapped in `if (process.env.NODE_ENV !== 'production')`. Also added the global `ValidationPipe` (`whitelist`, `forbidNonWhitelisted`, `transform`) and a global `common/filters/http-exception.filter.ts` here, since `03 Auth — API` is the first feature with DTOs/endpoints to validate and error-format.
- `common/filters/http-exception.filter.ts` uses `@Catch()` (not `@Catch(HttpException)`) so *every* unhandled error — not just ones we explicitly throw — gets converted to the `{ success: false, error }` shape `api-contract.md` mandates, including framework-level 404s for unmatched routes. Non-`HttpException` errors are logged with `Logger.error` and collapsed to a generic "Internal server error" message, per `code-standards.md`'s "never expose internals" rule.
- Logout has no server-side token invalidation — `architecture.md`'s schema has no refresh-token/blacklist table, and adding one wasn't in scope for this feature. Web logout clears the httpOnly cookie; mobile logout is a client-side `SecureStore.deleteItemAsync` (per `library-docs.md`), already documented as the mobile pattern. Revisit only if a future feature explicitly calls for server-side revocation.
- All four endpoints (`register`, `login`, `refresh`, `logout`) verified end-to-end against a live `nest start --watch` instance and the real local Postgres DB — not just `tsc`/lint: register → 201, login/refresh/logout → 200 (Nest defaults `@Post()` to 201, so `@HttpCode(HttpStatus.OK)` was added explicitly to the three non-creation endpoints), wrong password and missing/garbage refresh tokens → 401, duplicate email → 409, invalid DTO body → 400, unauthenticated logout → 401, unknown route → 404 — all in the `{ success, data?, error? }` shape.
- Nest's `start:dev` (`nest start --watch`) on this Windows setup occasionally spawns a new child process on file-change recompiles before the previous one has released port 3001, so a few `EADDRINUSE` crashes appear in dev server logs around rapid successive edits. The newest process always wins the port and serves correctly (verified — no functional impact), it's just log noise. Not investigated further since it's a local dev-tooling quirk unrelated to app code.
- **04 Auth — Web UI + Wiring.** `apps/web` runs on `localhost:3000`, `apps/api` on `localhost:3001` — genuinely cross-origin, not just cross-port-same-app. `main.ts` had no `enableCors()` call at all before this feature (feature 03 was only ever hit via curl/Swagger, never a browser), so the first browser `fetch()` from a web page would have been silently blocked. Added `app.enableCors({ origin: WEB_APP_URL, credentials: true })` in `main.ts` — `credentials: true` is required for the browser to accept/send the httpOnly refresh cookie cross-origin. New env var `WEB_APP_URL` (`http://localhost:3000` in dev) added to `apps/api/.env(.example)`, not previously in `code-standards.md`'s env table since nothing needed it until a browser was in the picture.
- **Middleware is `proxy.ts` in Next.js 16, not `middleware.ts`** (confirmed via Context7 docs for `next@16.2.9`, matches the installed `16.2.10` — see https://nextjs.org/docs/messages/middleware-to-proxy). File lives at `apps/web/proxy.ts` (project root, alongside `app/`), default-exports a `proxy()` function, same `NextRequest`/`NextResponse`/`config.matcher` API as the old `middleware.ts`.
- **`proxy.ts` cannot read the API's refresh-token cookie** — that cookie is `httpOnly` and scoped to the API's own origin (`localhost:3001`); a request to the web app's origin (`localhost:3000`) never carries it, so the Next.js server has no accessible signal of auth state at all (the access token is browser-memory-only, per `build-plan.md`, and is lost on every full navigation/reload anyway). Resolved by having `lib/auth.ts` set a second, plain (non-`httpOnly`) marker cookie — `subtrack_session`, `apps/web/lib/constants.ts` — on the web app's own origin whenever a session is established (login/register success, or a successful silent refresh), and clearing it on logout/refresh-failure. `proxy.ts` reads only this marker to decide whether to redirect `/dashboard`, `/subscriptions`, `/settings` → `/login` (no marker) or `/login`, `/register` → `/dashboard` (marker present). This is optimistic/UX-only routing, matching the "Optimistic Authentication" pattern Next's own docs recommend for this exact situation — real authorization still happens at the API on every request via the Bearer access token; the marker proves nothing to the API and is never trusted as a security boundary.
- Lazy/self-healing access-token refresh implemented exactly as `code-standards.md`'s `apiFetch` pattern describes: `apps/web/lib/api-client.ts`'s `apiFetch()` attaches the in-memory access token if present, and on a `401` (excluding `/auth/*` paths, to avoid recursion and to let login/register surface real "invalid credentials" errors instead of triggering a refresh attempt) calls `refreshAccessToken()` once, retries, then clears the session and hard-redirects to `/login` if the retry also fails. No separate "bootstrap on app load" step exists yet — the first protected data call after a reload naturally 401s (empty in-memory token) and self-heals via the refresh cookie. Flag for feature 05+: Server Component data fetching against protected endpoints isn't resolved — the access token lives in browser memory only, so an SSR `fetch` from a Server Component has no token to attach. Likely resolution is client-side (`"use client"`) data fetching for protected pages, but not decided now since feature 04 has no protected page content to fetch.
- Rewrote `apps/web/app/globals.css` from the stock `create-next-app`/shadcn scaffold to the full token set in `ui-tokens.md` (first UI-touching feature for web) — added the SubTrack `@theme` block verbatim (colors, category colors, radii, plus a `--shadow-xs` override matching `ui-tokens.md`'s exact Card box-shadow), and re-pointed the shadcn semantic vars (`--primary`, `--card`, `--muted`, `--border`, etc.) in `:root` at the SubTrack palette so the existing `Button`/`Card` primitives (from feature 01's `shadcn` scaffold) render on-spec without touching their source. Dropped the shadcn `.dark` block, `@custom-variant dark`, and the `accent`/`chart-*`/`sidebar-*` semantic slots entirely — no dark mode or sidebar is specified anywhere in `ui-tokens.md`/`ui-rules.md`, and shadcn's own `accent` slot was unused by `button.tsx`/`card.tsx` (also would have collided with SubTrack's own `--color-accent` brand token, same CSS custom property name, different meaning). `apps/mobile`'s `tailwind.config.js` mirror is deferred to Phase 6 (mobile) — nothing in `ui-tokens.md` changed, only implemented for the first time on web.
- `components/ui/input.tsx` was hand-written rather than pulled via the shadcn CLI, to hit `ui-rules.md`'s Form Input spec exactly (`border-border`, `rounded-md`, `px-3 py-2`, focus ring in `accent`) without fighting a generated default. `Button`/`Card` stayed as their existing shadcn-generated files — only their token inputs changed (see above).
- No dedicated `Navbar`/landing-page work done here — both are out of this feature's scope (`architecture.md` lists `components/layout/Navbar.tsx` but no numbered `build-plan.md` feature builds it yet; it needs `GET /users/me` data that no page fetches until later features). Root `/` (`app/page.tsx`) is still the unmodified `create-next-app` boilerplate — untouched, since no feature has claimed it yet.
- **Post-04 follow-up:** switched `LoginForm`/`RegisterForm` from hand-rolled `useState` + manual zod `safeParse` to shadcn/ui's `Form` pattern (`react-hook-form` + `zodResolver`), per explicit request. This project's shadcn registry/style (`components.json`'s `"base-vega"`) doesn't actually ship a `form.tsx` — `npx shadcn add form` / `add @shadcn/form --view` both resolve to zero files for this style — so `apps/web/components/ui/form.tsx` was hand-written to match the standard shadcn Form API, substituting a local `React.cloneElement` forwarder for Radix's `Slot` in `FormControl` (this project has no Radix dependency anywhere; Button/Card are Base UI, Input/Label are hand-written/plain). Added `react-hook-form` + `@hookform/resolvers` to `code-standards.md`'s approved `apps/web` dependency list as unavoidable peer dependencies of shadcn's `Form` component — same treatment as `passport` in feature 03. `Label` came through the CLI cleanly (`npx shadcn add label`) and needed no changes. Re-verified: `next build`/`eslint` clean on both forms, and the rendered HTML confirmed `FormLabel`'s `for` correctly targets `FormControl`'s generated `id` (React `useId`-based, via `FormItem`), with `aria-describedby`/`aria-invalid` wired per field.
- **Post-04 follow-up:** added a show/hide toggle to password fields (new `apps/web/components/ui/password-input.tsx`, wraps `Input` + a `lucide-react` `Eye`/`EyeOff` button, used by both `LoginForm` and `RegisterForm`) and fixed `Button` never showing a pointer cursor on hover — `buttonVariants`' base classes were missing `cursor-pointer`/`disabled:cursor-not-allowed` entirely (a gap in the original shadcn-generated `button.tsx` from feature 01, not something either of the first two features had introduced). Both re-verified via `next build`/`eslint` (clean) and rendered HTML (toggle button present with correct `aria-label`, submit buttons carry `cursor-pointer`).
- End-to-end verification was done via `curl` against both running dev servers (`nest start --watch` on 3001, `next dev` on 3000), not a real browser — no browser-automation tool (e.g. `chromium-cli`) is available in this Windows/Git-Bash environment. Verified: register (201, sets the httpOnly refresh cookie, correct CORS headers), login with correct/wrong password (200 / 401 with human-readable error), `/auth/refresh` off the cookie (200, new access token), authenticated `/auth/logout` (200), `/login` and `/register` server-rendering real form markup (not a blank/error page), and the full `proxy.ts` redirect matrix (`/dashboard`, `/subscriptions`, `/settings` → `/login` with no marker cookie; `/login` → `/dashboard` with the marker cookie present; protected route falls through to the page — a 404 today, since `/dashboard` itself isn't built until feature 11 — when the marker is present). Did **not** verify the client-side React flow (button click → `fetch` → in-memory token → `document.cookie` write → `router.push`) in an actual browser DOM — that part is standard, well-typed code that compiled and lint-passed clean, but visual/interactive confirmation is still outstanding. Flag for whoever picks up feature 05+: consider setting up `chromium-cli` (or running `/run-skill-generator`) so future UI features get real in-browser verification.
- **05 Subscriptions API.** Discovered (verified empirically via a throwaway TypeORM query, not assumed) that Postgres `numeric` columns come back from the `pg` driver as **strings**, even though `library-docs.md`'s entity pattern types `cost`/`amount` as `number` — e.g. `sub.cost` was `"15.99"` (`typeof === 'string'`) before any fix. `api-contract.md` types `cost`/`amount` as JSON numbers, so left unfixed this would have silently serialized as quoted strings over the wire. Fixed with a new reusable `common/utils/numeric.transformer.ts` (a `ValueTransformer` — `from` casts string→number, `to` is a pass-through) applied to `Subscription.cost` and `PaymentHistory.amount`. Pure TS/JS-side fix — no migration needed, column type (`numeric`) is unchanged. **Flag for feature 09**: `ExchangeRate.rate` has the exact same issue and was deliberately left untouched (out of this feature's scope) — apply `numericTransformer` there too when building the Currency Module.
- **05 Subscriptions API.** Response shapes are built via a private `toResponse`/`toPaymentHistoryResponse` mapper in `subscriptions.service.ts` (mirrors the `buildAuthResult` pattern already established in `auth.service.ts`) rather than returning the raw TypeORM entity — strips `userId`/`createdAt`, which aren't in `api-contract.md`'s documented shape.
- **05 Subscriptions API.** `calculateNextRenewalDate()` (`common/utils/billing-cycle.util.ts`) does UTC-based date math (parses `YYYY-MM-DD` as UTC midnight to avoid local-timezone drift) and clamps month/year overflow — e.g. Jan 31 + 1 month → Feb 28 (Feb 29 in a leap year), rather than JS `Date`'s default rollover to Mar 2/3. Same clamping applies advancing a Feb 29 start date by a year into a non-leap year → Feb 28. Covered by 10 unit tests in `billing-cycle.util.spec.ts`, verified in isolation with `npx jest billing-cycle` per `build-plan.md`'s "unit-testable in isolation" requirement. This same function will be reused unchanged by `renewal.job.ts` in feature 08 (advancing `nextRenewalDate` forward, not just deriving it from `startDate`) — same signature already supports both call shapes.
- **05 Subscriptions API.** `billingCycle: 'custom'` requires `customIntervalDays` — enforced twice: DTO-level via `@ValidateIf((dto) => dto.billingCycle === 'custom') @IsInt() @Min(1)` on `CreateSubscriptionDto` (returns a 400 with a human-readable message before the service layer ever runs), and again defensively in the service's `resolveNextRenewalDate()` for the `update` path, since `UpdateSubscriptionDto` (`PartialType`) makes every field optional and a client could send `billingCycle: 'custom'` while leaving an existing non-custom `customIntervalDays` (`null`) untouched.
- **05 Subscriptions API.** `GET /subscriptions` (status/search/pagination) is built with `subscriptionRepo.createQueryBuilder()` (`.andWhere` + `ILIKE` for case-insensitive name search + `.skip/.take` + `.getManyAndCount()`) rather than the repository's plain `find()` — first use of the query builder in this codebase; confirmed the pattern against current TypeORM docs via the context7 MCP server (`/llmstxt/typeorm_io_llms_txt`) rather than assuming from training knowledge, per `library-docs.md`'s "check MCP first" rule.
- **05 Subscriptions API.** Verified end-to-end against the live dev stack (`nest start --watch` on 3001) and the real local Postgres DB via `curl`, not just `tsc`/`jest`/`eslint`: full CRUD (create incl. weekly-cycle renewal-date math, list with `search`/`status`/`page`/`limit`, get-by-id incl. nested `paymentHistory`, update with `billingCycle` change triggering `nextRenewalDate` recalculation, cancel), DTO validation (`custom` cycle missing `customIntervalDays` → 400 with message), ownership scoping (random/unowned UUID → 404), auth boundary (no token → 401), and the delete-blocked-by-payment-history rule (manually inserted a `payment_history` row for a seeded subscription via a throwaway TypeORM script, confirmed `DELETE` on it returns 400 asking the user to cancel instead, then cleaned the row back out and re-ran `npm run seed` to restore a clean baseline). All new Swagger routes confirmed present at `/api/docs`.
- **05 Subscriptions API — dev environment note (not a code change):** while testing, found port 3001 already held by a stale process and killed it; while cleaning up two redundant `start:dev` trees started during testing, the pre-existing `npm run dev` (web+api) tree also went down (unclear whether it was already crashing from the earlier port contention or was killed as a side effect). Restarted via `npm run dev` from the repo root to restore both dev servers — no app code was affected, purely a local process-management hiccup, but noting it in case a future session sees an unexplained gap in dev-server uptime.
- **06 Subscriptions Page — Web UI (mock data).** Built entirely against `lib/mock-data.ts` (9 seeded subscriptions spanning all 5 categories, 3 currencies, all 4 billing cycles incl. one custom-interval, both statuses, and one with empty `paymentHistory` to exercise the empty state) — no API calls anywhere yet, per the build plan's mock-first principle. Add/Edit forms update local component state only (nothing persists across a reload); feature 07 replaces this with real `POST`/`PATCH /subscriptions` calls.
- New `Subscription`/`SubscriptionDetail`/`PaymentHistoryEntry`/`PaginatedSubscriptions` types added to `apps/web/types/index.ts`, mirroring `api-contract.md`'s Subscriptions section exactly (`Client Type Mirror` rule) — these will be reused unchanged by feature 07's real wiring and by `apps/mobile` in Phase 6.
- No Cancel/Delete UI built in this feature — `build-plan.md`'s feature 06 bullets (table, add form, detail view w/ edit form + payment history) don't mention them, and `api-contract.md`'s cancel/delete-block behavior is explicitly listed under feature 07 (wiring), not 06. Deferred `alert-dialog.tsx` (installed via the shadcn CLI batch add, then removed since nothing used it yet) to whenever feature 07 builds the real Cancel confirmation flow.
- `nextRenewalDate` for a newly-added mock subscription is set equal to `startDate` rather than computed — `architecture.md`'s invariant reserves that calculation for `common/utils/billing-cycle.util.ts` in `apps/api`, and `code-standards.md` forbids business logic in `apps/web` even for mock data. Same reasoning for edits: changing `billingCycle`/`startDate` in the mock edit form does not recompute `nextRenewalDate` locally.
- Installed shadcn `Table`, `Dialog`, `Select`, `Badge` (`npx shadcn add table dialog select badge`) — first use of any of these for web. Unlike `Form`, this style (`base-vega`) does resolve all four cleanly. Running the batch add also re-touched `button.tsx` (identical content) and, on a re-run while iterating, silently reverted the Post-04 `cursor-pointer`/`disabled:cursor-not-allowed` fix — caught immediately via `git diff`-equivalent inspection and re-applied. Full details on customization gotchas (esp. `SelectValue` not auto-labeling) are in `ui-registry.md`.
- **Playwright installed as an `apps/web` devDependency** (`playwright` + `npx playwright install chromium`) to get real in-browser verification of this feature — the gap flagged since feature 04 ("no `chromium-cli`-equivalent tool in this Windows/Git-Bash environment") had never been closed. Verified end-to-end in a headless Chromium session (not just `tsc`/`eslint`/`next build`): table renders with correct columns/badges, search and All/Active/Cancelled filters narrow the list correctly, Add Subscription dialog opens/validates/submits and the new row appears, row click navigates to the detail page, the detail page's pre-filled edit form and payment history (incl. the empty-state case for "Notion") render correctly, and `/subscriptions/<unknown-id>` returns a real 404. Caught and fixed one real bug this way: the category `<Select>`'s `SelectValue` was displaying the raw enum (`"entertainment"`) instead of its label ("Entertainment") — base-ui's `SelectValue` doesn't auto-label from `SelectItem` children, it needs an explicit function-child mapping value → label (now documented in `ui-registry.md`). Not something `tsc`/`eslint` could have caught, since it was a runtime-only rendering difference.
- **Post-06 follow-up:** added page `metadata`/`generateMetadata` to both subscriptions routes (browser tab titles were previously inherited unchanged from the root layout's "SubTrack") and converted the detail page's always-visible inline "Edit Subscription" form into an `EditSubscriptionDialog` modal (same shadcn `Dialog` + `SubscriptionForm` pattern as `AddSubscriptionDialog`), triggered by a new "Edit" button in the detail page header — requested to match the Add flow's UX rather than having two different edit affordances (inline vs. modal) on the same page. Re-verified with Playwright: both tab titles render correctly (including the dynamic `{name} | SubTrack` title on the detail page), and the modal opens pre-filled, saves, and closes correctly.
- **Post-06 follow-up:** added client-side pagination to the subscriptions table, `DEFAULT_PAGE_SIZE = 10` — requested explicitly. Installed shadcn `Pagination` and adapted its anchor-based `PaginationPrevious`/`PaginationNext` for local state (`preventDefault` + `onPageChange`, since there's no URL/router involved yet). Added 3 more mock subscriptions (`lib/mock-data.ts`, now 12 total) since the original 9 all fit on one page and pagination would've had nothing to demonstrate — first case where the mock dataset size itself mattered for a feature, not just its variety. Re-verified with Playwright: page 1 shows 10 rows, page 2 shows the remaining 2, Previous/Next disable correctly at each end, and changing the search or status filter resets back to page 1.
- Running `npx shadcn add pagination` re-touched `button.tsx` again and dropped the `cursor-pointer`/`disabled:cursor-not-allowed` fix a third time (see the same note under the 06 entry above) — this is now a confirmed recurring side effect of *any* shadcn CLI add, not a one-off. Whoever runs `npx shadcn add` next should diff `button.tsx` immediately afterward rather than assuming it's untouched.
- **Post-06 follow-up:** the detail page's "Edit" button had no visible styling — it used shadcn's plain `variant="outline"`, whose default `bg-background` is the exact same token as the page's own background, so the button only showed as an invisible-fill outline. Fixed by giving it an explicit `bg-accent text-surface hover:bg-accent-dark` override (Primary button treatment, per `ui-rules.md`), same pattern already used on `AddSubscriptionDialog`'s trigger button.
- **07 Subscriptions — Web Wiring.** New `apps/web/lib/subscriptions.ts` wraps `apiFetch` with `listSubscriptions`/`getSubscription`/`createSubscription`/`updateSubscription`/`cancelSubscription`/`deleteSubscription` — same one-wrapper-per-domain pattern as `lib/auth.ts`. New `CreateSubscriptionInput`/`UpdateSubscriptionInput` types added to `types/index.ts` mirroring `api-contract.md`'s `POST`/`PATCH /subscriptions` request bodies exactly (`Client Type Mirror` rule).
- **07 Subscriptions — Web Wiring.** Both `app/subscriptions/page.tsx` and `app/subscriptions/[id]/page.tsx` stay thin Server Components, but all data fetching moved into `SubscriptionsPageClient`/`SubscriptionDetailClient` as client-side `useEffect` fetches — this is the resolution to the gap flagged since feature 04 ("Server Component data fetching against protected endpoints isn't resolved"). The access token lives in browser memory only and is never available during SSR, so a Server Component fetch against a protected endpoint has no token to attach; client-side fetch-on-mount (with `isLoading`/`error` states) was the only viable option without introducing a new session-storage mechanism. `SubscriptionDetailClient` now takes `id: string` instead of a pre-fetched `subscription`/`SubscriptionDetail` prop. Since the subscription's name is no longer known at the server-metadata stage, `[id]/page.tsx`'s `generateMetadata` was replaced with a static `"Subscription - SubTrack"` title, and `SubscriptionDetailClient` sets `document.title` directly once its fetch resolves — a pragmatic client-side workaround, not the Metadata API. `lib/mock-data.ts` is deleted (no remaining references) now that both pages read from the real API.
- **07 Subscriptions — Web Wiring.** `SubscriptionDetailClient` detects a 404 by matching the API's exact error string (`"Subscription not found"`, the literal `NotFoundException` message in `SubscriptionsService.findOwnedOrThrow`) and calls `next/navigation`'s `notFound()` — confirmed via Playwright that `notFound()` works correctly when called from a Client Component (renders the real not-found boundary for `/subscriptions/<random-uuid>`), which the bundled Next 16 docs don't explicitly confirm either way for this app's version.
- **07 Subscriptions — Web Wiring.** `eslint-plugin-react-hooks@7`'s new `react-hooks/set-state-in-effect` rule flags the classic "`useCallback` fetch function called from a `useEffect`" shape as a direct setState-in-effect violation, even when the setState calls only run after an `await`. Fixed in both `SubscriptionsPageClient` and `SubscriptionDetailClient` by declaring the async fetch function *inside* the `useEffect` callback and invoking it there, rather than hoisting it to a `useCallback` referenced by the effect — this satisfies the linter and is also just a smaller diff. `SubscriptionsPageClient` still needs to trigger a re-fetch from outside the effect (after `AddSubscriptionDialog`'s `onCreated`), solved with a `refreshToken` counter state bumped by the callback and included in the effect's dependency array, rather than exposing the fetch function itself.
- **07 Subscriptions — Web Wiring.** List search is now debounced 300ms (`SEARCH_DEBOUNCE_MS` in `SubscriptionsPageClient`) before hitting the real `GET /subscriptions?search=` — necessary now that every keystroke would otherwise be a live network request, unlike the mock phase's instant client-side `.filter()`.
- **07 Subscriptions — Web Wiring.** Added Cancel and Delete actions to the subscription detail page (not present in the feature 06 mock UI, which the build plan explicitly deferred to this feature) — new `CancelSubscriptionDialog`/`DeleteSubscriptionDialog` components, both using the newly-installed shadcn `alert-dialog` (`npx shadcn add alert-dialog`, clean install, did not touch `button.tsx`). Both follow `ui-tokens.md`'s Destructive button spec verbatim (`bg-error`, literal `text-white`) and `ui-rules.md`'s "always requires a confirmation step before firing" rule — Cancel is only rendered when `status === "active"`; Delete is always rendered, with the server's exact payment-history-block message (`"This subscription has payment history and cannot be deleted — cancel it instead"`) surfaced inline in the dialog on failure, per `api-contract.md`.
- **07 Subscriptions — Web Wiring.** Verified end-to-end with Playwright against the live dev stack and real local Postgres DB (not just `tsc`/`eslint`/`next build`, all of which also pass clean): login → real subscriptions list (7 seeded rows) → server-side search/status filtering → Add Subscription (real `POST`, appears in the list) → detail page navigation with dynamic tab title → Edit (real `PATCH`, heading updates) → Cancel with confirmation (Keep vs. actual cancel, status badge updates, Cancel button disappears once cancelled) → Delete with no payment history (real `DELETE`, redirects to the list) → 404 for an unknown UUID. Separately verified the delete-block path by manually inserting a `payment_history` row for a seeded subscription via a throwaway TypeORM script (same approach as feature 05's verification), confirming the exact block message renders, then cleaning the row out and re-running `npm run seed` to restore a clean baseline. One live bug caught mid-verification and fixed: an early manual test run deleted the seeded "Netflix" row (it had no payment history, so the delete correctly succeeded) — not a code bug, just a reminder that Playwright runs against this feature mutate the real dev database; `npm run seed` restores it.
- **08 Renewal Scheduled Job.** New `modules/scheduler/` per `architecture.md`'s monorepo tree — `scheduler.module.ts` (registers `ScheduleModule.forRoot()` plus `TypeOrmModule.forFeature([Subscription, PaymentHistory])` directly, rather than importing `SubscriptionsModule`, since `RenewalJob` only needs repository injection, not `SubscriptionsService`'s user-scoped business logic — scheduled jobs deliberately query across all users) and `renewal.job.ts`, matching `code-standards.md`'s scheduled-job pattern verbatim: `@Cron('5 0 * * *')`, per-item try/catch so one failure never stops the batch, `Logger` calls on start and on completion with a renewed/total count. Confirmed the `@nestjs/schedule` `@Cron`/`ScheduleModule.forRoot()` API against current docs via the context7 MCP server (`/nestjs/schedule`) before installing, per `library-docs.md`'s "check MCP first" rule — installed `@nestjs/schedule@^6.1.3` (already on the approved dependency list in `code-standards.md`). Reused `calculateNextRenewalDate()` unchanged from `billing-cycle.util.ts`, confirming the "advance forward" call shape flagged as already-compatible back in feature 05.
- **08 Renewal Scheduled Job.** Manual trigger script at `apps/api/src/scripts/trigger-renewal.ts` (new `npm run trigger:renewal`, mirrors `seed.ts`'s `ts-node -r tsconfig-paths/register` invocation) — uses `NestFactory.createApplicationContext(AppModule)` to get a real Nest DI context and calls `app.get(RenewalJob).handleRenewals()` directly, rather than re-implementing the query/update logic standalone (unlike `seed.ts`, which talks to `AppDataSource` directly since it has no corresponding service). This means the manual trigger exercises the exact same code path the cron will run — not a separate implementation to keep in sync. Never exposed as a controller endpoint, per the build plan's explicit "never exposed as a public endpoint" instruction.
- **08 Renewal Scheduled Job.** Verified end-to-end against the live local Postgres DB by running `npm run trigger:renewal` (not just `tsc`/`eslint`/`nest build`, all of which also pass clean) — at the time of testing (2026-07-21) the seeded "REG Electricity Bill" and "Spotify" subscriptions already had a `next_renewal_date` in the past due to elapsed real-world time since they were seeded, so no fixture mutation was needed to exercise the due path. Confirmed via the `postgres` MCP server (read-only queries): both got a correct `payment_history` row (right `amount`/`currency`, `paid_at` = today) and had `next_renewal_date` advanced by exactly one billing cycle (not looped forward to catch up to today — matches the documented single-advance-per-run behavior, since the daily cron will pick up any remaining gap incrementally); the cancelled "Cloud Backup" subscription was correctly skipped despite also having a past `next_renewal_date`; untouched active subscriptions not yet due were left alone. Since the `postgres` MCP tool is read-only, cleanup (clearing the two new `payment_history` rows so `npm run seed` could re-run without hitting its FK constraint against `subscriptions`) required a throwaway TypeORM script, same pattern as features 05/07 — written, run, and deleted, then `npm run seed` restored the clean 7-subscription/0-payment-history baseline.

- **09 Currency Module + Exchange Rate Job.** `library-docs.md`'s documented FX provider, `exchangerate.host`, has since gone behind a paid/keyed access wall — a live curl during this feature confirmed it now returns HTTP 200 with `{ success: false, error: { code: 101, type: "missing_access_key" } }` on every request, not a 4xx that `!response.ok` would catch. Asked the user for direction rather than guessing; the first candidate replacement (Frankfurter, ECB-based, keyless) was verified live to have **zero African-currency coverage at all** — no RWF, which is this app's `users.base_currency` default and one of the 3 seeded subscription currencies, so it would have silently broken currency conversion for the app's primary use case. Landed on **`open.er-api.com`** (the free, keyless open-access tier of the same exchangerate-api.com data) after confirming live that it covers RWF both as a base and target currency. `EXCHANGE_RATE_API_URL` updated to `https://open.er-api.com/v6` in `.env`/`.env.example`; `library-docs.md` and `architecture.md`'s stack table updated to match. Request shape changed from `?base=` (query param) to `/latest/{base}` (path param), and the failure shape is `{ result: "error", "error-type": "..." }` at HTTP 200 rather than a `success`/`error.info` shape — `fetchLatestRates()` checks `data.result !== 'success'` explicitly rather than trusting `response.ok` alone (an initial implementation that only checked `response.ok` crashed with a raw `Cannot convert undefined or null to object` `TypeError` on `Object.entries(rates)` when `rates` was missing — fixed before ever reaching a real network edge case, since HTTP-200-with-error-body is a normal shape for this class of API, not a hypothetical).
- **09 Currency Module + Exchange Rate Job.** Resolved the upsert conflict-target mismatch flagged back in feature 05: `exchange_rates`'s unique index is `(base_currency, target_currency, (fetched_at::date))` — an **expression** index (the date-cast). TypeORM 1.x's type-safe `repository.upsert()`/`InsertQueryBuilder.orUpdate()` can only target a plain-column constraint (verified via the context7 MCP server, `/llmstxt/typeorm_io_llms_txt`, per `library-docs.md`'s "check MCP first" rule), and the old raw-string `.onConflict("...")` escape hatch documented in TypeORM's own `upgrading-from-0.3` guide was **removed** in the 1.x line this project is on — so neither API can express an expression-based arbiter index. Resolved with a hand-written parameterized `INSERT ... ON CONFLICT (base_currency, target_currency, ((fetched_at)::date)) DO UPDATE ...` via `exchangeRateRepo.manager.query()` (values still fully parameterized, no injection risk) — the same "hand-rolled over fighting the framework" precedent as `SnakeNamingStrategy`/the manual cookie parser. Did **not** change the migration or the documented schema constraint — this is a query-layer fix only. Verified empirically: ran the manual trigger script twice in a row against the live DB — 498 rows both times (3 in-use currencies × ~166 targets each), and `count(DISTINCT (base_currency, target_currency, fetched_at::date))` also came back 498, confirming the second run updated existing rows in place rather than duplicating or erroring.
- **09 Currency Module + Exchange Rate Job.** Applied `numericTransformer` to `ExchangeRate.rate` (the fix flagged as deferred back in feature 05, when it was applied to `Subscription.cost`/`PaymentHistory.amount`) — same pure TS-side fix, no migration needed since the column type is unchanged. Confirmed via the `postgres` MCP server that cached rates come back as real numbers (e.g. `1471.350212`), not strings.
- **09 Currency Module + Exchange Rate Job.** `CurrencyModule` (`modules/currency/`) owns `ExchangeRate` and also imports `TypeOrmModule.forFeature([Subscription])` directly (read-only distinct-currency query) rather than importing `SubscriptionsModule` — same cross-domain-read-without-importing-the-whole-module precedent `SchedulerModule` set in feature 08 for `RenewalJob`. `SchedulerModule` in turn imports `CurrencyModule` (not a direct repo injection) so `ExchangeRateJob` can call `CurrencyService.refreshAndCache()` — this one **does** match `library-docs.md`'s cron-registration example verbatim, since `CurrencyService` encapsulates real business logic (fetch + upsert), not just repository access. No `CurrencyController`/public endpoints exist — `api-contract.md` documents no currency routes, and the "never call the FX API from a request path" invariant means `CurrencyService` is scheduled-job-only for now; `getConvertedAmount()` (reads cache only) is implemented per `library-docs.md` but has no caller yet — feature 10's `DashboardService` is its first consumer.
- **09 Currency Module + Exchange Rate Job.** Manual trigger script at `apps/api/src/scripts/trigger-exchange-rate.ts` (new `npm run trigger:exchange-rate`), same `NestFactory.createApplicationContext(AppModule)` + direct job-method-call pattern as `trigger-renewal.ts` from feature 08 — exercises the exact same code path the `@Cron('0 */6 * * *')` schedule will run. Never exposed as a controller endpoint, per the build plan's instruction. No new npm dependencies were needed (native `fetch`, already available in the Node version this project targets).
- **09 Currency Module + Exchange Rate Job.** Verified end-to-end against the live dev stack and real local Postgres DB (not just `tsc`/`eslint`/`nest build`, all of which also pass clean): ran `npm run trigger:exchange-rate` twice via a temporarily-started `nest start --watch` instance, confirmed via the `postgres` MCP server that all 3 in-use currencies (EUR, RWF, USD — matching the seeded data) refreshed with 0 failures each run, spot-checked the EUR/RWF/USD triangle of rates for sane values and numeric (not string) types, and confirmed idempotency (see the upsert note above). Stopped the standalone dev server afterward (`taskkill` on the port-3001 PID) rather than leaving it running, to avoid the `EADDRINUSE`-on-restart issue already noted in feature 03's entry if a future session runs the root `npm run dev`.

- **10 Dashboard API.** `api-contract.md` documents `GET /dashboard/summary`'s response shape but not the `upcomingRenewals` time window — sourced that from `ui-rules.md`:147 ("Upcoming Renewals (7 days, count)") instead, since the build plan's UI stat card is the only place it's specified. Implemented as `nextRenewalDate` between today and today+7 days inclusive (UTC), original currency kept per the contract (`upcomingRenewals` amounts are documented as staying unconverted, unlike the totals/breakdown).
- **10 Dashboard API.** No existing utility normalized a subscription's cost to a common monthly figure (`billing-cycle.util.ts` only computes renewal *dates*), so added `common/utils/spend.util.ts` — `toMonthlyEquivalent()`, a new pure/unit-tested function (own `.spec.ts`, same one-util-one-spec precedent as `billing-cycle.util.ts`) using a 30.4375-day (365.25/12) average month length to normalize weekly/custom costs. `totalYearlySpend` is derived as `totalMonthlySpend * 12` rather than separately annualizing each subscription, so the two totals stay internally consistent by construction. This normalization formula isn't specified anywhere in `context/` — a judgment call, not a documented spec; revisit if product feedback wants a different definition of "monthly spend."
- **10 Dashboard API.** `CurrencyService.getConvertedAmount()` throws on a cache-miss; per `library-docs.md`'s rule ("fail that single conversion gracefully... never crash the whole dashboard response"), `DashboardService` wraps every conversion in a private `convertSafely()` that logs a warning and contributes `0` to the running total on failure, rather than letting one missing rate 500 the whole endpoint. In practice this should be rare since `ExchangeRateJob` (feature 09) refreshes rates for every currency actually in use.
- **10 Dashboard API.** `DashboardModule` (`modules/dashboard/`) owns no entities — imports `TypeOrmModule.forFeature([Subscription, PaymentHistory])` directly for read-only repo access, `CurrencyModule` for `CurrencyService.getConvertedAmount()` (its first real request-path caller, as flagged back in feature 09), and `UsersModule` for `UsersService.findById()` (to read `baseCurrency`) — same cross-domain-read-without-owning-the-module precedent `SchedulerModule` set in features 08/09, now reused a third time.
- **10 Dashboard API.** `GET /dashboard/spend-trend` builds its `months`-sized point array up front (ascending, ending at the current UTC month) and fills each from an in-memory aggregation of matching `payment_history` rows (fetched once via a single `paidAt >= <range-start>` query, grouped by month+currency in JS) rather than a SQL `GROUP BY` — kept the query simple since `payment_history` volumes are small (append-only, one row per renewal) and this avoids fighting TypeORM's query builder for a `to_char()`-based date-truncation group-by. Months with no payments still appear in the response as a `0` point, not omitted — matters for the chart's x-axis once feature 12 wires it up.
- **10 Dashboard API.** Verified end-to-end against the live dev stack (already-running `nest start --watch` on 3001) and the real local Postgres DB via `curl` + the `postgres` MCP server (not just `tsc`/`eslint`/`jest`/`nest build`, all of which also pass clean): logged in as the seeded test user, confirmed `GET /dashboard/summary`'s `categoryBreakdown` amounts sum exactly to `totalMonthlySpend`, `upcomingRenewals` correctly included only the 3 subscriptions due within 7 days, and confirmed `totalYearlySpend`'s minor rounding delta from `totalMonthlySpend * 12` (`1749911.21` vs. a naive `145825.93 * 12 = 1749911.16`) is expected floating-point behavior from deriving both totals from the same unrounded running total rather than a bug. `GET /dashboard/spend-trend` correctly returned all-zero points — confirmed via the `postgres` MCP server that `payment_history` currently has 0 rows (the clean baseline restored at the end of feature 08's verification), so this is a true empty state, not a bug. No auth → 401. Both routes confirmed present in `/api/docs-json`.
- **10 Dashboard API — dev DB cleanup (not a code change).** Verification also turned up 10 duplicate active "Netflix" subscriptions in the dev DB with a stale `next_renewal_date` (2026-01-31). Investigated via the `postgres` MCP server before assuming a bug: each belonged to a **different** throwaway user (`shot<timestamp>@example.com`, 10 distinct accounts, all created 2026-07-21 — Playwright test-fixture accounts from an earlier feature's browser-verification runs, never cleaned up), not the seeded test user — so contrary to an initial flag, they were **never actually included** in the test user's dashboard totals (queries are correctly scoped by `userId`); the flag's premise was wrong, though the DB clutter itself was real. Removed with a throwaway TypeORM script (`AppDataSource`, same write-run-delete pattern as features 05/07/08's cleanup scripts) that deleted only users matching `email ILIKE 'shot%@example.com'` and their subscriptions — left `bruce@gmail.com` (0 subscriptions, looks like the user's own manual testing, not a Playwright artifact) untouched. DB now holds exactly the 2 real users and the 7 seeded subscriptions.

- **11 Dashboard Page — Web UI (mock data).** Found a real gap while reading `api-contract.md` against `ui-rules.md` before building: the documented `GET /dashboard/summary` response has no field the "Active Subscriptions" stat card could read (`categoryBreakdown` only carries dollar amounts, not a count). Fixed by updating `api-contract.md` first (added `activeSubscriptionsCount: number`, per `architecture.md`'s "update the contract file first" invariant), then patching the already-shipped feature 10 `DashboardService.getSummary()`/its `SummaryResponse` type to actually return it (`activeSubscriptions.length` — the array was already being fetched, no new query). Re-ran the API's `tsc`/`eslint`/`jest` (still 16/16) and re-curled the live endpoint to confirm `activeSubscriptionsCount: 7` appears for real before building the web UI against it — building mock data against an unfixed contract would have just meant re-doing this in feature 12.
- **11 Dashboard Page — Web UI (mock data).** Installed `recharts` for the first time in this project (`^3.10.0`, approved in `code-standards.md`) and checked its docs live via the context7 MCP server rather than trusting `library-docs.md`'s example or training data — found that `Cell` (the classic per-slice-pie-color component most Recharts tutorials use) is now a deprecated no-op stub in the actually-published v3.10.0. Used the still-working alternative instead: Recharts' `Pie`/`Bar` default shapes read a `fill` field directly off each datum, so `CategoryBreakdownChart` maps `data` to include `fill: CATEGORY_CHART_COLORS[entry.category]` per item rather than nesting `<Cell>` children. Full gotcha + reasoning recorded in `ui-registry.md` so the next chart built in this project (mobile's `react-native-chart-kit` is a different library, but a second Recharts usage on web would hit the same trap) doesn't have to rediscover it.
- **11 Dashboard Page — Web UI (mock data).** `ui-rules.md` requires a gradient fill beneath the spend-trend line (`ui-tokens.md`: `#0F9D78` stroke, 3px, `rgba(15,157,120,0.15)` fill) that `library-docs.md`'s bundled example (a bare `<LineChart>`/`<Line>`) doesn't produce — built `SpendTrendChart` as an `<AreaChart>`/`<Area>` with an SVG `<linearGradient>` instead, which renders identically to a line chart (`dot={false}`, single series) while actually satisfying the gradient-fill spec.
- **11 Dashboard Page — Web UI (mock data).** `/dashboard/page.tsx` was kept a plain Server Component — no `"use client"`, no `DashboardPageClient` wrapper — since this mock-only feature has zero interactive/mutable state (no forms or dialogs on this page, unlike `/subscriptions`), so `code-standards.md`'s Server-Components-by-default rule needed no exception. Only the two Recharts leaf components (`CategoryBreakdownChart`, `SpendTrendChart`) are `"use client"`, since Recharts requires a browser runtime. Flag for feature 12: real API wiring plus the build plan's explicit "independent loading states per card/chart" requirement will very likely force at least one client boundary onto the page — expect that refactor there, not before.
- **11 Dashboard Page — Web UI (mock data).** Two distinct empty-state layers implemented, both per `ui-rules.md`: (1) page-level — `DashboardEmptyState` (icon + muted text + "Add your first subscription" CTA linking to `/subscriptions`) replaces the entire stat-cards/charts/renewals body when `activeSubscriptionsCount === 0`; (2) chart-level — `SpendTrendChart`/`CategoryBreakdownChart` each independently render muted text instead of an empty/zeroed chart, per the explicit "never render an empty/zeroed chart" rule. The chart-level case matters for a real, not hypothetical, scenario: feature 10's own live verification showed `GET /dashboard/spend-trend` returning 6 real zero-amount points for a user with active subscriptions but no `payment_history` yet (a zero-filled array, not an empty one) — `SpendTrendChart`'s guard checks `points.every(p => p.amount === 0)`, not `points.length === 0`, specifically to catch this. Both empty-state branches were verified live via Playwright by temporarily editing `mock-dashboard-data.ts` in place (zero subscriptions; separately, zeroed spend-trend points with subscriptions still present), screenshotting each, then reverting the file back to its shipped values — no permanent "empty" mock variant was kept in the codebase, since nothing needs to switch between datasets at runtime yet.
- **11 Dashboard Page — Web UI (mock data).** Verified in an actual headless Chromium session via Playwright (not just `tsc`/`eslint`/`next build`, all of which also pass clean), per this project's established browser-verification convention (`playwright` already a devDependency since feature 06): logged the auth-bypass cookie (`subtrack_session`, the documented UX-only marker `proxy.ts` checks — see the memory note on this) rather than a real login flow, confirmed all 4 stat cards / section headings / category legend rows (with correct labels and percentages) / upcoming-renewal rows (correct original-currency amounts, dates, and 2 SVGs rendered for the two charts) render with real values matching the mock dataset, then separately verified both empty-state branches (above). Screenshots visually confirmed token-correct styling (category donut colors matching `--color-category-*`, accent-teal gradient beneath the trend line, warning-colored urgency pills on all 3 renewals since they're all ≤7 days out). Script + screenshots were throwaway (written into `apps/web/`, run, then deleted), consistent with the documented Playwright-verification gotchas (Node module resolution requires running from inside `apps/web/`, not a scratchpad).

- **12 Dashboard — Web Wiring.** New `apps/web/lib/dashboard.ts` wraps `apiFetch` with `getDashboardSummary()`/`getSpendTrend(months = 6)` — same one-wrapper-per-domain pattern as `lib/auth.ts`/`lib/subscriptions.ts`. `/dashboard/page.tsx` reduced to a thin Server Component (mirrors `/subscriptions/page.tsx`'s shape from feature 07); all state and both fetches moved into a new `DashboardPageClient` (`components/dashboard/DashboardPageClient.tsx`) — the client-boundary refactor flagged as likely back in feature 11 did land here, exactly as predicted. `lib/mock-dashboard-data.ts` deleted (no remaining references).
- **12 Dashboard — Web Wiring.** `getDashboardSummary()` and `getSpendTrend()` are fired from two separate `useEffect`s with independent `isLoading`/`error` state, per the build plan's explicit "don't block the whole page on one slow section" requirement — this is the one place the requirement has real teeth, since `/dashboard/summary` alone backs three sections at once (stat cards, category breakdown, upcoming renewals) as a single payload, while `/dashboard/spend-trend` is a genuinely separate request. The page-level empty-state decision (`DashboardEmptyState` vs. the full dashboard) unavoidably gates on the summary fetch resolving first, since `activeSubscriptionsCount` isn't known until then; the spend-trend chart's own loading/error state stays fully independent of summary's and never blocks or is blocked by it.
- **12 Dashboard — Web Wiring.** Did not re-verify the page-level zero-subscriptions empty state against the live API for this feature — that branch's logic (`summary.activeSubscriptionsCount === 0 → <DashboardEmptyState />`) is unchanged from feature 11, where it was already verified live via Playwright against mocked zero data; forcing a real zero-subscriptions state here would have meant mutating the seeded test user's live subscriptions (cancel/delete all 7) purely for a verification pass, which wasn't worth the DB churn given the condition itself is a one-line unchanged check. Judgment call — revisit if this decision turns out wrong.
- **12 Dashboard — Web Wiring.** Verified end-to-end in a headless Chromium session via Playwright against the live dev stack (`nest start --watch` on 8000, `next dev` on 3000, real local Postgres DB) — not just `tsc`/`eslint`/`next build`, all of which also pass clean. This time logged in through the real `/login` form (not the `subtrack_session` bypass cookie used for feature 11's mock-only pass), since real API calls need a real access token in memory. Confirmed all 4 stat cards render live-computed values (`RWF 145,826` monthly / `RWF 1,749,911` yearly / `7` active / `3` upcoming), the category breakdown pie + legend renders with real percentages, and the upcoming renewals list shows real rows in original currency. Caught a genuine (not simulated) empty-state case in the process: `payment_history` is currently empty in the dev DB (the clean baseline restored at the end of feature 08's verification, still holding), so `GET /dashboard/spend-trend` returned all-zero points for real, and the chart correctly rendered "No spend history yet." instead of a zeroed chart — validates the build plan's empty-state requirement against real data rather than a mock fixture. Screenshot confirmed layout/styling; script + screenshot were throwaway (written into `apps/web/`, run, then deleted).

- **13 Settings Page — Web (Full, incl. v2 scaffolding).** `GET /users/me`/`PATCH /users/me` didn't exist yet (`UsersModule` had only a service, no controller) — added `UsersController` + `UpdateUserDto` (`name`/`baseCurrency`, both optional) following the exact same thin-controller/`{success,data}` pattern as `SubscriptionsController`. `UsersService` gained `getProfile()`/`updateProfile()` + a private `toProfileResponse()` mapper (mirrors `SubscriptionsService`'s `toResponse()` precedent) returning `{id, name, email, baseCurrency, monthlySpendLimit}` per `api-contract.md`.
- **13 Settings Page — Web.** **Real bug found and fixed while building `updateProfile()`:** the obvious `Object.assign(user, dto)` pattern (copied from `SubscriptionsService.update()`'s existing precedent) silently drops any field *not* included in a partial `PATCH` body from the **response** — e.g. `PATCH /users/me` with only `{"name": "..."}` came back with `baseCurrency` missing from the JSON entirely. Root cause: this project's `tsconfig.json` targets `ES2023` (`useDefineForClassFields` defaults on), so a DTO class field declared `baseCurrency?: string;` compiles to an own property explicitly set to `undefined` on construction — `class-transformer`'s `plainToInstance()` (which Nest's global `ValidationPipe` uses) instantiates the DTO via `new UpdateUserDto()` first, so an unset optional field is `undefined` *as an own enumerable property*, not simply absent. `Object.assign` then overwrites the real entity value with that `undefined`. Verified via the `postgres` MCP server that this is a **response-only** bug, not data corruption — TypeORM's `repository.save()` skips genuinely-`undefined`-valued properties when building its `UPDATE` SQL, so the DB column itself was never touched; only the JS object returned to the controller (and thus the JSON response) was wrong. Fixed in `UsersService.updateProfile()` with explicit per-field `if (dto.x !== undefined) user.x = dto.x` assignments instead of `Object.assign`. **Flag for a future session:** `SubscriptionsService.update()` (feature 05) has the exact same `Object.assign(subscription, dto)` pattern and is very likely affected identically — it's unexercised today only because `apps/web`'s `SubscriptionForm`/`EditSubscriptionDialog` always submit every field on every edit (zod schema requires the full shape), never a true partial body. Not fixed in this session — out of scope for a Settings feature, and touching `SubscriptionsService` wasn't necessary to ship `PATCH /users/me` correctly — but worth fixing with the same per-field-assignment pattern whenever that module is next touched, since any future partial-PATCH caller (a mobile screen that only edits one field, an API consumer) would hit it.
- **13 Settings Page — Web.** Built the `Navbar` (`components/layout/Navbar.tsx`) that `ui-rules.md` has specified since feature 04, deliberately deferred both times (see the feature 04 and feature 06/07 decisions above) because it depends on `GET /users/me`, which no page fetched until now. Introduced a new `app/(app)/` route group (`layout.tsx` renders `Navbar` + `<main>`) so the chrome exists once, not per-page; `dashboard/`, `subscriptions/`, `subscriptions/[id]/` moved in unchanged (route groups don't affect URL paths — confirmed via `next build`'s printed route table). Added one thing beyond `ui-rules.md`'s literal spec: a small "Log out" button next to the avatar, since `lib/auth.ts`'s `logout()` had no caller anywhere in the app before this — without it there was truly no way to sign out from the UI. Full details (including a Windows `git mv` file-lock gotcha hit while moving the `subscriptions/` directory, and a navbar/profile-edit sync bug found and fixed with a small custom-event pub/sub) are in `ui-registry.md`.
- **13 Settings Page — Web.** `ProfileSection` (name + base currency, editable; email read-only) follows the same `react-hook-form`+`zodResolver` shape as `SubscriptionForm`, saved via the new `lib/users.ts` (`getCurrentUserProfile`/`updateCurrentUserProfile`, same one-wrapper-per-domain convention as `lib/subscriptions.ts`/`lib/dashboard.ts`). `SpendLimitSection`/`NotificationsSection` are the v2-scaffolded sections `build-plan.md`/`ui-rules.md` call for — disabled inputs/toggles + a `bg-info-light`/`text-info-foreground` "Coming in v2" `Badge`, wired to nothing. New shadcn `Switch` (`npx shadcn add switch`) installed clean, no side effects on `button.tsx` this time. A real runtime crash (`useFormField must be used within <FormField> and <FormItem>`, only caught via Playwright's `pageerror` listener — `tsc`/`eslint`/`next build` all stayed green through it) was found and fixed for the read-only Email field; details in `ui-registry.md`.
- **13 Settings Page — Web.** Verified end-to-end via a headless Chromium/Playwright session against the live dev stack (`nest start --watch` on 8000, `next dev` on 3000, real local Postgres DB) — 24 assertions covering: navbar nav links + active-state coloring + user name/avatar from a real `GET /users/me`, Settings navigation, Profile form pre-fill (name + read-only email) and save (dirty-gated Save button, real `PATCH /users/me`), the navbar picking up the renamed user **immediately** (the custom-event fix) and after a reload, both v2 sections' badges/disabled controls, and the full logout → redirect-to-`/login` → protected-route-redirect flow. Also directly verified via `curl` that `PATCH /users/me` returns the complete object for both a name-only and a baseCurrency-only partial body (the bug above). Test user's `name`/`baseCurrency` were mutated repeatedly during verification and confirmed back at the clean seeded baseline (`"Test User"`/`"RWF"`) via a final `curl` check — no `npm run seed` re-run was needed this time. Script + screenshot were throwaway (written into `apps/web/`, run, then deleted), same convention as every prior UI feature's Playwright verification.
- **14 Mobile Auth.** `apps/mobile/app`, `components`, `hooks`, and `constants` were still the untouched stock `create-expo-app` demo scaffold (tabs/explore/animated-splash/light-dark theme system) — feature 01 had only flattened the folder *structure* to match `architecture.md`, never replaced the actual screens. Deleted all of it (`app/index.tsx`, `app/explore.tsx`, `components/app-tabs*`, `components/animated-icon*`, `components/{external-link,hint-row,themed-text,themed-view,web-badge}.tsx`, `components/ui/collapsible.tsx`, `hooks/use-color-scheme*`, `hooks/use-theme.ts`, `constants/theme.ts`) before building real screens. `app.json`'s `userInterfaceStyle` changed `automatic` → `light` and the splash `backgroundColor` from the template's blue (`#208AEF`) to `#F6F8F7` (`--color-background`) — `ui-tokens.md`/`ui-rules.md` define one light theme only, so the stock dark-mode-aware chrome and off-brand splash color didn't match anything being built.
- **14 Mobile Auth.** Per `apps/mobile/AGENTS.md`'s explicit "Expo HAS CHANGED, read the exact versioned docs" instruction, checked the context7 MCP server (`/websites/expo_dev`) against SDK 57 before writing any router/auth code, per `library-docs.md`'s MCP-first authority order. Confirmed the current recommended auth pattern is `Stack.Protected` (`<Stack.Protected guard={isAuthenticated}><Stack.Screen name="(tabs)" /></Stack.Protected>`, and the inverse for `(auth)`) from `docs.expo.dev/router/advanced/authentication` — not the older manual `<Redirect>`-in-nested-layout pattern `library-docs.md`'s Expo Router section (written pre-SDK-57) doesn't mention either way. Router redirects automatically on a `guard` flip, so `(auth)/login.tsx`/`register.tsx` need no manual `router.replace()` after a successful sign-in.
- **14 Mobile Auth.** `lib/auth.ts` (pure token functions: `login`/`register`/`logout`/`refreshAccessToken`, in-memory access token + `expo-secure-store` refresh token) and `lib/auth-context.tsx` (a new `SessionProvider`/`useSession` React Context) are split into two files — `architecture.md`'s mobile tree only lists one `lib/auth.ts`. Mobile needs a reactive piece web never did: `Stack.Protected`'s `guard` prop needs live React state, whereas web's equivalent (`proxy.ts`) reads a cookie per-request with no client-side reactivity required. `lib/api-client.ts` mirrors web's `apiFetch` shape exactly (same 401-triggers-one-silent-refresh-then-gives-up pattern, same `/auth/*`-path recursion guard) but adapted for no cookies — mobile's `/auth/refresh` sends `{refreshToken}` in the body per `api-contract.md`. Closed a gap web doesn't have: when a *later* authenticated call's silent refresh fails (refresh token expired/revoked while the app was authenticated), `apiFetch` has no React tree to redirect from directly — added a small hand-rolled pub/sub (`subscribeToSessionInvalidation`/an internal `Set` of listeners in `lib/auth.ts`) that `SessionProvider` subscribes to on mount, flipping `isAuthenticated` false so `Stack.Protected` reacts — same "hand-rolled over fighting the framework" precedent as web's `PROFILE_UPDATED_EVENT`, adapted since React Native has no `window` to dispatch a DOM event on. Not exercised by anything in this feature (login/register need no token, logout is excluded from the retry branch) — forward-looking scaffolding `code-standards.md`'s api-client contract already requires, for feature 15+'s authenticated calls.
- **14 Mobile Auth.** `SessionProvider` bootstraps on mount by reading the persisted refresh token from `SecureStore` and attempting one silent refresh; `isLoading` (distinct from `isAuthenticated`) gates the root layout's splash screen so it stays visible until *both* Inter fonts are loaded and this bootstrap resolves — prevents a flash of the login screen for an already-logged-in user on a warm start. `/auth/refresh` only returns `{accessToken}` (no `user`), so a cold-start-authenticated session has `user: null` until a later screen fetches its own data — same gap web accepted in feature 04 ("no bootstrap-on-load step"), not fixed here since nothing in this feature's scope needs the profile eagerly (the placeholder Dashboard/Settings screens degrade to a generic heading when `user` is `null`).
- **14 Mobile Auth.** Installed `@expo-google-fonts/inter` (new, added to `code-standards.md`'s approved mobile deps) since `ui-tokens.md` requires Inter on mobile too and it ships as **separate static files per weight**, not a variable font — unlike web's single `--font-sans` + `font-medium`/`font-semibold` utility split. Added one `fontFamily` key per weight actually used (`font-sans`/`-medium`/`-semibold`/`-bold` → `Inter_400Regular`/`500Medium`/`600SemiBold`/`700Bold`) to `apps/mobile/tailwind.config.js`; components must use these instead of Tailwind's `font-weight` utilities, which React Native mostly ignores for custom static-weight fonts. Documented in `ui-tokens.md`'s Mobile Token Mirror section.
- **14 Mobile Auth.** `ui-tokens.md`'s own Mobile Token Mirror code block was missing `accent-foreground` (`#ffffff`, Primary button text) even though web's `globals.css` already had it — an oversight predating any mobile UI work. Added it to both `tailwind.config.js` and `ui-tokens.md` itself (per that doc's own "must never drift" invariant) rather than falling back to a hardcoded `text-white`, since a real token slot for it makes more sense now that mobile is actually building Primary buttons. Also added `apps/mobile/constants/colors.ts` — a plain-value mirror of the same tokens, camelCased, for native props that can't take a NativeWind `className` (`TextInput`'s `placeholderTextColor`, `ActivityIndicator`'s `color`) — replacing the deleted stock `constants/theme.ts`. `library-docs.md` already anticipated needing exactly this ("`categoryColorMap` sourced from the mobile token file") for `react-native-chart-kit`; this is that file, built now instead of later since `Input`/`Button` needed it first.
- **14 Mobile Auth.** No icon library is an approved mobile dependency, so `PasswordInput`'s show/hide toggle is a plain text `Pressable` ("Show"/"Hide") rather than an eye icon like web's `PasswordInput`. `@expo/vector-icons` ships bundled with Expo (no install needed) but wasn't reached for, to avoid introducing icon usage into the codebase's visual language for a single button before any feature actually needs an icon set broadly — revisit when one does. Similarly, the branded header's logo mark is a flat `bg-accent` fill, not web's `linear-gradient` — `expo-linear-gradient` isn't an approved dependency and NativeWind doesn't render CSS gradients on native views.
- **14 Mobile Auth.** Built a minimal `(tabs)` group (`_layout.tsx` + `dashboard.tsx`/`subscriptions.tsx`/`settings.tsx`, 3 tabs, no icons) even though `build-plan.md`'s feature 14 scope is only login/register/token-handling/root-guard — `Stack.Protected` needs a real, renderable route to redirect an authenticated user *to*, and unlike web (whose `proxy.ts` can leave `/dashboard` genuinely unbuilt and fall through to Next's own 404), Expo Router has no equivalent soft-landing for a route the app's own code deliberately navigates to. Each screen is a "Coming soon" placeholder; `settings.tsx` additionally has a Sign Out button so the guard is actually testable end-to-end (log in → land on tabs → sign out → back to login). The full tab bar (4 items incl. the prominent center "Add" button per `ui-rules.md`) is deferred to feature 15, since "Add" has no destination screen until then. Filenames match `architecture.md`'s already-declared tree exactly, so feature 15/16/17 replace these bodies in place rather than restructuring.
- **14 Mobile Auth — initial verification gap, later resolved (see the "Infra: fixed the expo-router hoisting bug" entry below).** At the time feature 14 shipped, neither a simulator/emulator nor a physical device was available in this Windows environment, and `expo start`/`expo start --web` both crashed before serving anything. **What was verified instead, at ship time:** `npx tsc --noEmit` and `npx expo lint` both clean; zod's field-path error-mapping logic (`issue.path[0]` → `FieldErrors`) confirmed directly against the installed `zod@4.4.3` with both an invalid and valid payload; and the exact request/response shapes `lib/auth.ts`/`lib/api-client.ts` assume were confirmed via `curl` against the live running API (`register`/`login`/`refresh`/`logout`, plus 401-wrong-password and 409-duplicate-email) — all matched `lib/types.ts` exactly. **Still not verified after the fix below:** actual on-screen rendering, `Stack.Protected`'s redirect behavior, focus/error styling, and the `PasswordInput` toggle's absolute-position offset (`right-3 top-9`, a guess never confirmed against real layout) — the dev server now *runs* cleanly, but nothing has connected a real client (Expo Go/simulator) to it yet to exercise the UI. Revisit when a future mobile feature needs real visual verification.
- **Infra: fixed the expo-router hoisting bug that broke `expo start` entirely (user-reported, not tied to a numbered feature).** The user hit `npm run dev:mobile` crashing with `Cannot find module 'expo-router/_ctx-shared'` — the same class of issue flagged as an unresolved "verification gap" at the end of feature 14, but that entry underestimated it: it broke plain `expo start` (not just `--web`), a real blocker, not just a missing-verification note. Root cause fully diagnosed this time (via `npm explain`, not guessing): `apps/mobile/package.json` pins an **exact** `react`/`react-dom` version (`19.2.3`, no `^`/`~`) — required because React Native's New Architecture needs an exact match — which differs from the root's hoisted `19.2.4` (pulled in by `apps/web`'s Next.js). npm correctly keeps `react`/`react-dom`/`react-is`/`expo-router` nested under `apps/mobile/node_modules` rather than hoisting them; this is **correct, load-bearing behavior, not a bug** — forcing a hoist (tried via `npm install`/`npm uninstall`+reinstall/`npm dedupe`, none of which relocated it anyway) would risk breaking the exact-version requirement. The actual bug: `@expo/cli`'s own bundled `@expo/router-server` package (hoisted to the workspace root as part of `@expo/cli`) does a plain Node `require("expo-router/...")` from deep inside root `node_modules` — and Node's CJS resolution only walks *upward* through ancestor `node_modules` directories, never sideways into `apps/mobile/node_modules`, so it always failed to find it. Confirmed two symptoms of the same root cause: `startTypescriptTypeGenerationAsync` crashing the whole process on startup (fatal, killed `expo start` outright), and — once that was dodged by temporarily disabling the `typedRoutes` experiment — `createServerRouteMiddleware`/`fetchRouterManifest` continuously spamming `Metro error: Cannot find module 'expo-router/internal/routing'` every ~2 seconds (non-fatal but made the dev server unusable). **Fix:** a Windows directory junction at `node_modules/expo-router` pointing at the real `apps/mobile/node_modules/expo-router` — Node resolves *through* a junction to the real path before doing any further lookups, so `expo-router`'s own nested dependencies (`react-is@19`, etc.) still resolve relative to its real location under `apps/mobile`, not the root; the exact-react-version pin is completely undisturbed. Verified this is the complete fix, not a partial one: re-ran `npm run dev:mobile` with `typedRoutes` restored to `true` and watched the log for 35+ seconds — zero errors, both the native entry point and the SSR route-manifest renderer bundle successfully on every request. Made durable with a new root `scripts/link-expo-router.js` (wired to `package.json`'s `postinstall`) that recreates the junction if missing — plain junctions aren't tracked by git and would silently vanish after `rm -rf node_modules && npm install` or a fresh clone; the script is idempotent (checks via `fs.lstatSync` before creating) and a no-op if `apps/mobile/node_modules/expo-router` doesn't exist yet. `apps/mobile/tsconfig.json`'s `include` array now flips between including/excluding `.expo/types/**/*.ts`/`expo-env.d.ts` depending on whether `expo start` last ran with typed routes actually working — confirmed this is Expo's own CLI auto-managing that file (not a bug in ours), so the committed `tsconfig.json` is left in whatever state it's in rather than fought; a separate `apps/mobile/types/css.d.ts` (`declare module "*.css";`) was added so `tsc --noEmit`'s `@/global.css` side-effect import type-checks regardless of which way Expo has flipped that file. One pre-existing, non-fatal Metro warning remains and was left alone: `Require cycle: lib/auth.ts -> lib/api-client.ts -> lib/auth.ts` — the same mutual-import shape `apps/web`'s `lib/auth.ts`/`lib/api-client.ts` already use (Next/webpack just doesn't warn about it); Metro's own message says cycles are allowed, so this is cosmetic, not a defect.
- **Infra: API dev port changed from 3001 to 8000** (user request, not tied to a numbered feature). Updated everywhere the port was configured or documented: `apps/api/.env`, `apps/api/.env.example` (`PORT` + its comment), `apps/web/.env.local`, `apps/web/.env.example`, `apps/mobile/.env`, `apps/mobile/.env.example` (`NEXT_PUBLIC_API_URL`/`EXPO_PUBLIC_API_URL`), `apps/web/lib/constants.ts`'s comment, `context/api-contract.md`'s documented base URL, `context/library-docs.md`'s Swagger docs URL, and `README.md`'s dev-script table. Also fixed a latent bug noticed while touching this: `main.ts`'s fallback was `process.env.PORT ?? 3000` — wrong even before this change, since 3000 is the *web* app's port, not the API's; it had never been hit in practice because `.env` always set `PORT` explicitly. Now `?? 8000`, consistent with the real default. Historical entries elsewhere in this file that mention `localhost:3001` are left as-is — they're an accurate record of what was true when that work happened, not live documentation. Found and killed a stale orphaned `node .../apps/api/dist/main` process that had been squatting on port 8000 from an earlier ad-hoc `node dist/main.js` verification (see feature 09's Notes) — would have collided with the new dev port. Verified end-to-end after the change: `npm run start:dev` binds cleanly to 8000, `GET /api/docs` and `POST /auth/login` both work against the new port, `tsc`/`eslint` clean on `main.ts`. `apps/web`'s CORS-related `WEB_APP_URL` (still `http://localhost:3000`) is unrelated to this change and was left alone.

- **Logo rollout (user-provided assets, not tied to a numbered feature).** The user dropped in real brand assets (`apps/web/public/subtrack.png`, `apps/mobile/assets/images/subtrack.png`) to replace the placeholder gradient-"S" logomark used on both platforms' auth screens since feature 04/14. The source PNGs (1408×768, no real alpha channel — confirmed via PNG chunk inspection, no `tRNS`) turned out to be flattened exports with a baked-in light gray/white checkerboard "transparency indicator" pattern instead of actual transparency — a common export mistake, not something previously visible without inspecting raw pixels. Rebuilt both into a real transparent asset using `sharp` (already present in the root `node_modules`, no new dependency): keyed out low-saturation (checkerboard/glow) pixels to alpha 0 by a `max(r,g,b)-min(r,g,b) < 20` threshold (the icon itself is saturated teal, well clear of that band), manually computed the opaque-pixel bounding box (sharp's own `.trim()` didn't reliably find it — needs `.raw()` chained after `.trim()` before `.toBuffer()`, easy to miss), padded ~12% for breathing room, and downscaled to 512×512. Both app copies were overwritten in place at their original paths/filenames so `/subtrack.png` stays the canonical reference. Wired into `apps/web/app/(auth)/layout.tsx` (replacing the gradient "S" box) and, newly, into `apps/web/components/layout/Navbar.tsx` (no logomark existed there before — added since a nav bar with zero brand mark read as incomplete once a real logo existed) via `next/image`; and into `apps/mobile/app/(auth)/login.tsx`/`register.tsx` via `expo-image`'s `Image` (already an approved mobile dependency, not newly added). Some faint checker residue remains inside the ring's hollow center at full resolution (the pale mint "glow" halo in the source art overlapped the checkerboard's saturation range and couldn't be cleanly separated) — invisible at the 28-36px render sizes actually used, not worth further precision for a logomark this small.
- **15 Mobile Subscriptions.** Installed `@expo/vector-icons` via `npx expo install` (ships bundled with the Expo SDK, zero native-linking risk, works in Expo Go) — added to `code-standards.md`'s approved mobile deps. Needed because `ui-rules.md`'s mobile tab bar spec mandates a real icon for the prominent center "Add" button ("larger icon in an accent-colored circle"), which text alone can't satisfy, plus back/edit affordances to match web's `lucide-react` icon language. Used the `Feather` icon set specifically since `lucide-react` is itself a Feather fork — closest visual parity available without a custom icon font.
- **15 Mobile Subscriptions.** Backfilled four token gaps into `tailwind.config.js`/`constants/colors.ts`/`ui-tokens.md`'s Mobile Token Mirror that this feature was the first mobile feature to need: `border-light` (list-row dividers in `PaymentHistoryList`) and `success-foreground`/`warning-foreground`/`error-foreground`/`info-foreground` (status and renewal-urgency badge text — web's `StatusBadge`/`RenewalUrgencyPill` use these exact `-foreground` shades, not the base token, for correct contrast). Same "backfill only when a feature needs it" precedent as `accent-foreground` in feature 14.
- **15 Mobile Subscriptions.** New `components/ui/SelectField.tsx` — a hand-rolled bottom-sheet-style picker (`Pressable` field + `Modal` + `FlatList` of options) for Currency and Category, since React Native has no native `<select>` and no picker library is on the approved dependency list. Matches `ui-rules.md`'s explicit "picker mobile" requirement for the currency input. Billing Cycle (4 fixed options) and the subscriptions list's status filter instead reuse the same inline segmented-control pattern web already established (a row of `Pressable`s, active one filled `bg-accent`) rather than the modal picker, since a modal is unnecessary friction for a fixed 3-4-option toggle that fits on one row.
- **15 Mobile Subscriptions.** `components/ui/Input.tsx`'s `label` prop was made optional (previously required, `label: string`) so the subscriptions list's search field could render as an icon+placeholder input with no visible label — same visual pattern as web's `SubscriptionFilters` search box (`lucide-react` `Search` icon absolutely positioned inside the field). Backward compatible — every existing caller already passes `label`.
- **15 Mobile Subscriptions.** `SubscriptionForm` uses the same manual-`zod`-`safeParse`-into-`FieldErrors` pattern `(auth)/login.tsx`/`register.tsx` established in feature 14, not `react-hook-form` — that library is an `apps/web`-only approved dependency (a shadcn `Form`-component peer dependency with no mobile equivalent), and mobile has had no reason to introduce a form library since plain `useState` + `zod.safeParse` has handled every mobile form so far. Numeric fields (`cost`, `customIntervalDays`) are kept as raw strings in component state (`keyboardType="decimal-pad"`/`"number-pad"`) and converted via `Number(...)` immediately before validation, rather than trying to keep them typed as numbers through every keystroke — mirrors how `SubscriptionForm` on web already special-cases `valueAsNumber` for the same reason.
- **15 Mobile Subscriptions.** Start Date originally shipped as a plain text field (`YYYY-MM-DD`, auto-inserting dashes as digits are typed) rather than a native date-picker component, to avoid the autolinking risk of a native module. **Superseded immediately** — the user tried it and confirmed manual date entry was exactly the rough, unguessable UX the tradeoff was worried about, so it was worth the dependency after all. See the Post-15 follow-up entry below.
- **Post-15 follow-up.** Replaced the manual-text Start Date field with a real native picker: installed `@react-native-community/datetimepicker` via `npx expo install` (auto-registered its own config plugin in `app.json`, no manual native config needed) and built `components/ui/DateField.tsx`. Per the library's own current docs (confirmed via the context7 MCP server, `/react-native-datetimepicker/datetimepicker/v8.4.4`, not assumed from training knowledge): Android uses the recommended **imperative** `DateTimePickerAndroid.open()` API (a native dialog that needs no on-screen picker element at all), while iOS renders the `<DateTimePicker mode="date" display="inline" />` component — since iOS has no equivalent built-in modal dialog, it's wrapped in the same bottom-sheet `Modal` pattern `SelectField` already established, with a "Done" button to commit the draft selection (iOS's inline picker fires `onChange` continuously as the user scrolls, so a draft-then-commit step avoids updating form state on every intermediate scroll frame). `SubscriptionForm`'s `startDate` zod rule simplified back to `z.string().min(1, ...)` (matching web's schema exactly) now that the value only ever comes from a real `Date`, never free-typed text — the format/range validation this replaced is no longer reachable input.
- **Post-15 follow-up — real bug found and fixed.** Editing a subscription and saving showed stale data on the detail/list screens until the user navigated away and back in. Root cause: both screens fetched exactly once in a `useEffect` keyed on stable dependencies (`[id]` for the detail screen; `[status, debouncedSearch, page]` for the list) — `router.back()` after a save reuses the already-mounted previous screen instance rather than remounting it, so neither effect re-ran. Unlike web, where Edit is an in-page `Dialog` that updates local state directly via an `onSaved` callback, mobile's Edit is a separate stack screen (per `architecture.md`) with no such prop path back. Fixed by swapping each screen's fetch trigger to `useFocusEffect` (exported directly by `expo-router`, no new dependency): the detail screen's existing fetch effect was wrapped as-is in `useCallback`/`useFocusEffect` in place of `useEffect`; the list screen kept its original `useEffect` (still needed for live filter/search/page changes while the screen stays focused) and gained one small additive `useFocusEffect` that bumps a `refreshKey` counter included in that effect's dependency array, mirroring the `refreshToken` pattern web's `SubscriptionsPageClient` already uses for the same "force a refetch externally" need.
- **15 Mobile Subscriptions.** The tab bar's prominent center "Add" button (`ui-rules.md`: four items, Add visually prominent in an accent circle) has no real screen of its own inside `(tabs)/` — the actual add/edit screen lives at `app/subscription/add.tsx`, outside the tab group, per `architecture.md`'s declared tree and `library-docs.md`'s "screens outside `(tabs)/` render without the tab bar, with a back button instead" rule. Added `app/(tabs)/add.tsx` as a route file purely so `Tabs` has a slot to render (not in `architecture.md`'s original tree — same class of justified deviation as `lib/token-store.ts`/`lib/auth-context.tsx` in feature 14): its `tabBarButton` is swapped for a custom raised circular `Pressable` and its `tabPress` event is preempted with `event.preventDefault()` before any navigation happens, so the file's own component (a bare `Redirect` to `/dashboard`) is never actually reached in normal use — it's only a safety net for react-navigation's state restoration.
- **15 Mobile Subscriptions.** `app/subscription/add.tsx` handles both create and edit — an optional `?id=` search param switches it into edit mode (fetches and prefills via `getSubscription`, submits via `updateSubscription` instead of `createSubscription`) rather than adding a separate `edit.tsx` file, since `architecture.md`'s declared mobile tree only lists `add.tsx` and `[id].tsx` under `app/subscription/`. The detail screen's Edit button navigates to `/subscription/add?id={id}`.
- **15 Mobile Subscriptions.** Cancel and Delete on the detail screen use React Native's native `Alert.alert` (per `ui-rules.md`'s explicit "native confirm alert" mobile pattern and `build-plan.md` feature 15's own wording), not a custom modal — `Alert.alert`'s multi-button API (`{ text, style: "cancel" | "destructive", onPress }`) maps directly onto web's Keep/Confirm `AlertDialog` pattern, with `style: "destructive"` giving the confirm button the platform's native red-text treatment for free.
- **16 Mobile Dashboard.** Installed `react-native-svg` alongside `react-native-chart-kit` via `npx expo install` — an unavoidable peer dependency (chart-kit renders to SVG under the hood), not previously called out in `code-standards.md`'s approved mobile dep list even though `react-native-chart-kit` itself was pre-approved since feature 09. Confirmed the current `LineChart`/`PieChart` prop APIs (`chartConfig`, `bezier`, `hasLegend`, `fillShadowGradient*`) against the context7 MCP server (`/indiespirit/react-native-chart-kit`) before writing any chart code, per `library-docs.md`'s MCP-first rule — matched `library-docs.md`'s own documented `PieChart` pattern exactly, no drift found.
- **16 Mobile Dashboard.** `CategoryBreakdownChart` renders chart-kit's `PieChart` with `hasLegend={false}` and a hand-built legend list below (category dot + name + percentage) rather than chart-kit's built-in side legend — the built-in legend only shows `name`, with no slot for the percentage `ui-rules.md`'s "legend below with category name + percentage" rule requires, so reusing web's exact legend markup (adapted to `View`/`Text`) was the closer match. `SpendTrendChart` fakes web's gradient-fill-beneath-the-line look via chart-kit's `fillShadowGradientFrom/To` + opacity ramp (there's no direct `<Area>` equivalent in this library) and needed a local `accentRgba`/`textMutedRgba` helper — chart-kit's `chartConfig.color`/`labelColor` are opacity-aware functions, not the plain CSS-variable strings web's Recharts config takes, so the `Colors` hex tokens couldn't be passed through unchanged.
- **16 Mobile Dashboard.** Added `formatMonthLabel`/`formatCompactAmount` to `lib/format.ts` and `CATEGORY_CHART_COLORS`/new `lib/dashboard.ts` — direct mirrors of web's `lib/format.ts`/`lib/subscription-options.ts`/`lib/dashboard.ts`, except `CATEGORY_CHART_COLORS` sources plain hex from `constants/colors.ts`'s `Colors` object instead of web's `var(--color-category-*)` CSS custom properties, since chart-kit needs literal color values it can pass straight into SVG fill/stroke props.
- **16 Mobile Dashboard.** Both `DashboardStats` (2×2 grid, two `flex-row` pairs rather than web's CSS Grid — NativeWind's grid utilities aren't reliable per `ui-rules.md`'s own NativeWind caveat) and `StatCard` (26px stat number per `ui-rules.md`'s explicit mobile-scaled-down rule, vs. web's 30px) follow the same data shape as web's dashboard components one-for-one; `UpcomingRenewalsList` reuses the existing mobile `RenewalUrgencyPill` (built in feature 15) unchanged. The dashboard screen replaces the feature-14 placeholder in `app/(tabs)/dashboard.tsx` entirely and reuses the exact independent-loading-per-section pattern from web's `DashboardPageClient` (feature 12) — separate `summaryLoading`/`spendTrendLoading` state so a slow spend-trend query never blocks the stat cards from appearing — plus the same `useFocusEffect`-driven `refreshKey` pattern feature 15's post-ship bug fix established for the subscriptions list, so the dashboard reflects a subscription just added/edited/cancelled without requiring an app reload.
- **16 Mobile Dashboard — verification gap, consistent with feature 15's explicit user direction.** `npx tsc --noEmit` and `npx expo lint` both clean (one `StatCard.tsx` unused-`View`-import warning caught and fixed). Did **not** start a competing `expo start` instance to bundle-check this feature — unlike feature 14/15, live `node` processes were already listening on ports 8000/8081/8090 at verification time (the user's own dev servers, running since before this session's work began), and starting a second Metro instance risked colliding with or disrupting that already-running session rather than adding a redundant check. Since the user is doing all mobile visual/interactive testing themselves this phase (established in feature 15), Fast Refresh against their already-running dev server is the actual verification path here — no separate confirmation needed from this session. Real on-device verification (pie/line chart rendering, 2×2 stat grid spacing, gradient-fill look, focus-triggered refresh) is outstanding — same category of gap as every mobile feature since 14.
- **15 Mobile Subscriptions.** No screens in this feature were visually verified in a simulator/device — per explicit user direction, the user is doing all mobile visual/interactive testing themselves this session rather than delegating it to a Playwright-equivalent flow. Verified instead: `npx tsc --noEmit` and `npx expo lint` both clean across `apps/mobile`; a temporary `expo start` run (killed after) confirmed Metro bundles every new/changed file without error and regenerated Expo Router's typed-routes file to include the new `subscription/add`, `subscription/[id]`, and `(tabs)/add` routes (needed to even typecheck navigation calls like `router.push(\`/subscription/${id}\`)`); `apps/web`'s `next build` also re-verified clean end-to-end after the logo/Navbar changes. Real on-device verification (tab bar layout, modal picker feel, keyboard behavior, native Alert styling) is outstanding — see the testing notes handed to the user for what to check.
- **17 Mobile Settings.** Final build-plan feature. Added `lib/users.ts` and `UserProfile`/`UpdateUserInput` to `lib/types.ts` — direct mirrors of `apps/web`'s `lib/users.ts`/`types/index.ts`, calling the same `GET`/`PATCH /users/me` endpoints feature 13 already built and verified on the API side; no API changes needed for this feature. New `components/settings/` directory (mirrors `apps/web/components/settings/`) — not in `architecture.md`'s originally declared mobile tree, same class of justified per-feature deviation as `components/dashboard/` (feature 16) and `components/subscriptions/` (feature 15).
- **17 Mobile Settings.** `lib/auth-context.tsx`'s `SessionContextValue` gained an `updateUser` method (`= setUser` under the hood). Needed because, unlike web (where `Navbar` independently re-fetches `/users/me` on a `PROFILE_UPDATED_EVENT` `window` event), mobile's Dashboard header ("Hi, {name}") reads `user.name` straight from the session context, which is only ever populated once at login/register. Without this, editing your name in Settings would show the new name on the Settings screen itself but leave the Dashboard header stale until the next full sign-in. `ProfileSection`'s `onSaved` callback now calls both local `setProfile` and the session's `updateUser`.
- **17 Mobile Settings.** `ProfileSection` (Name + read-only Email + Base Currency) uses the same manual-`zod`-`safeParse`-into-`FieldErrors` pattern as `SubscriptionForm` (feature 15) — mobile has no `react-hook-form` (web-only approved dependency). Save button disabled until dirty (`name !== profile.name || baseCurrency !== profile.baseCurrency`), mirroring web's `!form.formState.isDirty` gate exactly, just computed manually since there's no form library tracking it here.
- **17 Mobile Settings.** `SpendLimitSection`/`NotificationsSection` are static, disabled-input v2-scaffolded sections per `ui-rules.md` (`bg-info-light`/`text-info-foreground` "Coming in v2" badge — both tokens already existed in `tailwind.config.js`/`constants/colors.ts` since feature 15/16's dashboard/subscription badge work, no backfill needed this time). Notifications' toggle uses React Native's built-in `Switch` (`disabled`, `value={false}`) rather than a custom component — first use of `Switch` in this codebase, no new dependency. Added a small shared `components/ui/ComingSoonBadge.tsx` since the identical badge markup is needed in both sections verbatim (unlike most mobile UI so far, which has stayed feature-specific).
- **17 Mobile Settings.** Settings screen header now shows the real avatar-circle-plus-name pattern `ui-rules.md` specifies ("same avatar pattern as web" — `bg-accent-light` circle, `text-accent` initial letter, name alongside), replacing the placeholder centered "Settings / Coming soon" text from feature 14. Sign Out (not itself part of any numbered feature's spec, but the only way to flip the auth guard back to unauthenticated for testing, per the original feature-14 placeholder's own note) is kept, now at the bottom of the scrollable content below the v2 sections rather than centered alone.
- **17 Mobile Settings — verification.** `npx tsc --noEmit` and `npx expo lint` both clean across `apps/mobile`. Did **not** start a competing `expo start` instance — the user's own dev servers were already listening on 3000 (web) and 8081/8090 (Metro) at verification time, same reasoning as feature 16 (avoid colliding with an already-running session; Fast Refresh against it is the real verification path since the user tests mobile UI themselves). The API dev server (port 3001) was not running at verification time, so no live `curl`/in-app round-trip against `/users/me` was possible from this session — the endpoint itself was already verified end-to-end in feature 13's web wiring, and this feature only adds a second client against the same unchanged contract. Real on-device verification (avatar/name header, dirty-save gate, currency picker, disabled v2 sections, Switch styling) is outstanding — same category of gap as every mobile feature since 14.

---

## Notes

- `.env.example` templates added for all three apps (`apps/web`, `apps/api`, `apps/mobile`) matching the env var table in `code-standards.md`. `apps/web/.gitignore` blanket-ignored `.env*`; added a `!.env.example` exception so the template stays committed.
- Verified end-to-end: `npm run build:web` (Next.js production build), `npm run build:api` (Nest build), and `npx tsc --noEmit` + `expo export --platform web` (bundle-only) for `apps/mobile` all succeed.
- `apps/api` DB workflow: `npm run migration:generate -- src/database/migrations/<Name>` (diffs entities against the DB), `npm run migration:run`, `npm run migration:revert`, `npm run seed` — all run from `apps/api/`. Entities live one-per-module under `modules/*/entities/`; the full entity list for `TypeOrmModule`/the CLI `DataSource` is centralized in `apps/api/src/database/entities.ts` so both stay in sync. Verified end-to-end: migration generated from entities, ran clean against a fresh local `subtrack` Postgres database, seed script populated it, and `npm run build` + a live `node dist/main.js` boot both succeeded with `TypeOrmModule` connecting successfully.