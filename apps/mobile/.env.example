# Exposed to the app bundle — API base URL. See context/code-standards.md.
# Use your machine's LAN IP instead of localhost when testing on a physical device.
EXPO_PUBLIC_API_URL=http://localhost:3001
